> [🇬🇧 English](./EXECUTION_LOG.md) | 🇵🇱 Polski

# Log wykonania FIXES_PLAN_v2_PL.md

**Data wykonania:** 2026-04-27
**Wersja:** 1.0 (auto-mode execution)

Ten plik dokumentuje wszystkie zmiany wdrożone w odpowiedzi na review z `FIXES_PLAN_v2_PL.md`. Format: ID znaleziska → status → pliki dotknięte → krótki opis.

---

## Faza P0 (przed go-live) — WSZYSTKIE WDROŻONE

| ID | Status | Pliki | Opis zmiany |
|----|--------|-------|-------------|
| **F-01** | ✅ DONE | `scripts/install_grid_silent.sh`, `scripts/install_db_silent.sh` | `set -euo pipefail`, usunięte `|| true`, jawny `exit $?` po `gridSetup`/`runInstaller` z komunikatem do logów inwentarza. |
| **F-02** | ✅ DONE | `sql/tac_full_readiness_26ai.sql`, `sql/tac_configure_service_rac.sql` | Dodano walidację `failover_restore=LEVEL1` w sekcji 4 (listing) i 5 (PASS/WARN/FAIL); generator srvctl z `-failover_restore LEVEL1`. |
| **F-03** | ✅ DONE | `scripts/setup_observer.sh` (nowy), `scripts/setup_observer_infra01.sh` (wrapper), `docs/07_FSFO_Observers_PL.md` (sekcja 6), `docs/09_Test_Scenarios_PL.md` (Scenariusz 5) | Sparametryzowany installer Observerów (master/backup); doc 07.6 dla `obs_dc`/`obs_dr`; scenariusz "awaria Master Observera" w 09. |
| **F-04** | ✅ DONE | `scripts/create_standby_broker.sh`, `scripts/setup_observer.sh`, `scripts/ssh_setup.sh` | `LAB_PASS` z `/root/.lab_secrets`; lokalny wallet auto-login dla DGMGRL; heredoc zamiast `su -c "...'$LAB_PASS'..."`. |
| **F-05** | ✅ DONE | `sql/legacy_19c/` (nowy katalog + README) | Pliki 19c (`validate_environment.sql`, `tac_full_readiness.sql`, `tac_replay_monitor.sql`, `fsfo_check_readiness.sql`, `fsfo_monitor.sql`) przeniesione do `sql/legacy_19c/` z README "DO NOT RUN". |
| **F-06** | ✅ DONE | `sql/tac_configure_service_rac.sql` | `dba_services` → `cdb_services` (con_id > 1); generator z `-pdb APPPDB`; nagłówek "Oracle 23ai/26ai EE". |
| **F-07** | ✅ DONE | `kickstart/ks-prim01.cfg`, `kickstart/ks-prim02.cfg` | `firewall --enabled --service=ssh --port=1521,1522,6200,27015 --trust=enp0s8 --trust=enp0s9`. |
| **F-08** | ✅ DONE | `response_files/dbca_prim.rsp` | `db_domain=lab.local` w `initParams`. |
| **F-09** | ✅ DONE | `src/TestHarness.java`, `src/pom.xml` (nowy) | Trzystopniowy fallback hasła: env `APP_PASSWORD` → env `LAB_PASS` → wbudowany lab-default `Oracle26ai_LAB!` (konwencja labu — jedno hasło wszędzie, zob. `01_Architektura` sekcja 2). `catch (SQLRecoverableException)` osobno od `SQLException`/`Exception`, status loop counter; pom.xml z `ojdbc11/ucp11/ons/simplefan/oraclepki` 23.5.0.24.07. |
| **F-10** | ✅ DONE | `docs/08_TAC_and_Tests_PL.md` (sekcja 3.0) | Pre-flight DDL: `CREATE USER app_user`, `GRANT KEEP DATE TIME / SYSGUID`, `CREATE TABLE app_user.test_log`. |
| **F-11** | ✅ DONE | `scripts/validate_env.sh` (nowy) | Pre-flight: DNS, NTP, SSH equivalency, mountpoints, ASM disks, porty Oracle, HugePages, swap/RAM, THP, memlock. Output PASS/WARN/FAIL z exit-code. |
| **F-12** | ✅ DONE | `scripts/setup_tac_services.sh` | `srvctl config service` check → `modify` zamiast `add` jeśli istnieje; flagi w tablicy bash; `set -euo pipefail`. |
| **F-13** | ✅ DONE | `docs/08_TAC_and_Tests_PL.md` (Krok 3 + 3.a) | Konfiguracja `ons.config` na stby01 (Oracle Restart, NIE GI); systemd unit `oracle-ons.service` dla persystencji. |
| **F-15** | ✅ DONE | `sql/tac_configure_service_rac.sql` | Usunięto `failover_method` z SELECT (TAF artefakt, nie ma efektu w TAC 26ai). |
| **F-18.A** | ✅ DONE | `kickstart/ks-prim01.cfg`, `kickstart/ks-prim02.cfg` | `vm.nr_hugepages=2200`, `vm.hugetlb_shm_group=54322`; `oracle/grid hard memlock unlimited`. |
| **F-18.B** | ✅ DONE | `response_files/dbca_prim.rsp` | `use_large_pages=ONLY`, `lock_sga=TRUE` w `initParams`. |
| **F-18.C** | ✅ DONE | `scripts/vbox_create_vms.ps1` (nowy) | `--paravirtprovider kvm`, `--nictype1..3 virtio`, `--hostiocache on` tylko dla infra01, RAM 8/9/9/6/3 GB. |
| **F-18.D** | ✅ DONE | `kickstart/ks-prim01.cfg`, `ks-prim02.cfg`, `ks-infra01.cfg` | `nmcli connection modify "System enp0s9" 802-3-ethernet.mtu 9000`. |
| **F-18.E** | ✅ DONE | `scripts/tune_storage_runtime.sh` (nowy) | mq-deadline na sdb, `emulate_write_cache=1` na lun_data1/lun_reco1 (NIE OCR), iSCSI initiator timeouts. |
| **F-18.F** | ✅ DONE | `docs/10_Performance_Tuning.md` (nowy) | Tabela mapy optymalizacji, procedura wdrożenia, benchmark przed/po, lista NIE-rekomendowanych, multipath iSCSI opcjonalnie. |
| **F-18.G** | ✅ ZWERYFIKOWANE | `docs/01_Architecture_and_Assumptions_PL.md` | RAM już zgodny z planem: infra01 8 GB, prim01/02 9 GB, stby01 8 GB, client01 3 GB. |
| **F-19** | ✅ DONE | `scripts/install_db_silent.sh` | `pushd $DB_HOME / popd` zamiast `cd`. |
| **F-20** | ✅ DONE | `scripts/ssh_setup.sh` | Guard `id -u != 0` na początku skryptu. |
| **F-21** | ✅ DONE | `docs/01_Architecture_and_Assumptions_PL.md` (sekcja 4.1) | Tabela wymagań licencyjnych: EE, RAC, Active Data Guard, Diagnostic+Tuning Pack, Advanced Security. |
| **F-22** | ✅ DONE | `docs/07_FSFO_Observers_PL.md` (Krok 6) | Noticebox Threshold vs LagLimit, kontekst MaxAvailability/SYNC vs MaxPerformance/ASYNC. |
| **F-23** | ⚠️ REVERTED (S28-52) | `response_files/client.rsp` | ~~`oracle.install.option=INSTALL_CLIENT`, `oracle.install.client.rootconfig.executeRootScript=false`~~ — te klucze NIE są w `rspfmt_clientinstall_response_schema_v23.0.0` (to klucze DB schema). Schema 23.0.0 client jest strict → INS-10105. Patrz S28-52 + VMs/FIXES_LOG FIX-070. |
| **F-24** | ✅ DONE | `response_files/dbca_prim.rsp` | Komentarz FIX-030 nad `templateName=New_Database.dbt`. |
| **F-25** | ✅ DONE | `sql/tac_configure_service_rac.sql` | Komentarze Expected output: `commit_outcome` z `'YES'` (słownik 26ai). |

---

## P2 — pozostawione jako decyzje zespołu / dalsze prace

| ID | Status | Powód odłożenia |
|----|--------|-----------------|
| **F-14** | ⏸ DECYZJA | `compatible=23.4.0` zachowane (komentarz w dbca_prim.rsp). Podniesienie do `23.26.0` jest jednokierunkowe — wymaga decyzji zespołu po stabilizacji środowiska. |
| **F-16** | ⏸ WERYFIKACJA | Kolumny `REPLAY_*` w `gv$session` — wymagana kontrola przy realnym 26ai (nazwy nie potwierdzone w docs publicznie). |
| **F-17** | ⏸ KOMENTARZ | Kickstart `ks-stby01.cfg` — komentarz architektoniczny (brak enp0s9). Niski priorytet, do dopisania w iteracji P2. |
| **Multipath iSCSI / tmpfs backstore** | ⏸ OPCJA | Opisane w `docs/10_Performance_Tuning.md` sekcja 6 i 5; nie wdrożone — wymagają zaawansowanej decyzji. |

---

## Nowe pliki dodane do projektu

| Plik | Wielkość (linie) | Opis |
|------|------------------|------|
| `scripts/setup_observer.sh` | ~180 | Sparametryzowany installer Observerów (master/backup). |
| `scripts/vbox_create_vms.ps1` | ~155 | PowerShell tworzący 5 VM z paravirt KVM + virtio + hostiocache. |
| `scripts/tune_storage_runtime.sh` | ~140 | Runtime storage tuning (mq-deadline, write-back, iSCSI initiator). |
| `scripts/validate_env.sh` | ~125 | Pre-flight sanity check (DNS/NTP/SSH/mounty/HugePages/THP). |
| `scripts/tac_service_resume.sh` | ~80 | Post-failover helper (DBMS_SERVICE.START_SERVICE na nowym primary - kompensacja braku Grid CRS na stby01). |
| `src/pom.xml` | ~75 | Maven build z pin'em ojdbc11/ucp11 23.5.0.24.07. |
| `docs/10_Performance_Tuning.md` | ~165 | Pełna dokumentacja optymalizacji (tabela, procedura, benchmark, NIE-rekomendowane). |
| `sql/legacy_19c/README.md` | ~30 | Ostrzeżenie DO NOT RUN dla reliktów 19c. |
| `EXECUTION_LOG_PL.md` | (ten plik) | Log wykonania FIXES_PLAN_v2_PL. |

## Rozszerzenia po pierwszym przeglądzie wykonania

| ID | Pliki | Opis zmiany |
|----|-------|-------------|
| **Tests-rewrite** | `docs/09_Test_Scenarios_PL.md` (200 → 460 linii) | Pełen pre-flight checklist (7 punktów + workaround MOUNTED PDB), wallet/grep gotchas, FIX-094 (STARTUP MOUNT + OPEN RO po SWITCHOVER TO PRIM), FIX-095 (`tac_service_resume.sh` na non-Grid stby), FIX-096 (StaticConnectIdentifier z PORT=1522), kill specific server process w Scen.3, oczekiwany flow w log Observera, reinstate, scenariusz 6 readiness, summary table 6 scenariuszy. |
| **FIX-096** | `scripts/create_standby_broker.sh` | Po `ADD DATABASE STBY` / przed `ENABLE CONFIGURATION` dodano explicit `EDIT DATABASE/INSTANCE SET PROPERTY 'StaticConnectIdentifier'='...PORT=1522...'` dla STBY + PRIM1 + PRIM2. Bez tego po switchover broker daje ORA-12514 (port 1521 zamiast 1522). |
| **FIX-095 helper** | `scripts/tac_service_resume.sh` | Idempotent helper: sprawdza role bazy (PRIMARY?), serwis aktywny w PDB, w razie potrzeby uruchamia `DBMS_SERVICE.START_SERVICE('myapp_tac')` z poprawną formą (lowercase, bez .lab.local). Wywoływany po Scenariusz 1 i 2 na nowym primary stby01. |
| **ONS persystencja** | `scripts/setup_cross_site_ons.sh` | Auto-skrypt teraz tworzy `oracle-ons.service` systemd unit na stby01 (nie tylko `onsctl start`). Po reboocie ONS wstaje automatycznie. |
| **Oracle Restart awareness** | `scripts/setup_tac_services_stby.sh` (nowy), `scripts/setup_tac_services.sh` (extended), `docs/08_TAC_and_Tests_PL.md` (Krok 1.5), `docs/09_Test_Scenarios_PL.md` (pre-flight punkt 8 + sek. 1.3/2.6 łagodzone) | Stby01 ma **Grid Infrastructure for Standalone Server (Oracle Restart)** — CRS na poziomie hosta. Po zarejestrowaniu serwisu MYAPP_TAC w Oracle Restart z `-role PRIMARY` (nowy skrypt + Krok 1.5 manual), CRS sam startuje serwis po failover/switchover. `tac_service_resume.sh` zostaje jako fallback gdy rejestracja w Restart została pominięta. RTO scenariuszy 1-2 spada z ~60 s do ~5–45 s. |
| **Active Data Guard trwały** | `scripts/create_standby_broker.sh` (krok 7 nowy), `docs/06_Data_Guard_Standby.md` (sekcja Active DG trwały), `docs/09_Test_Scenarios_PL.md` (pre-flight 6/6.a, sekcja 0.3 zmieniona na fallback, FIX-094 noticebox złagodzony) | STBY ma **zawsze** być w `READ ONLY WITH APPLY` (Real-Time Query). `create_standby_broker.sh` po stworzeniu STBY wykonuje: APPLY-OFF → `ALTER DATABASE OPEN READ ONLY` (jeśli MOUNTED) → `ALTER PLUGGABLE DATABASE ALL OPEN READ ONLY` + `SAVE STATE` → `srvctl modify database -startoption 'READ ONLY'` → APPLY-ON. Skutek: po reboot stby01 i po `SWITCHOVER TO PRIM` baza otwiera się natychmiast w READ ONLY (zamiast MOUNT) — broker NIE prosi operatora o `STARTUP`. FIX-094 staje się fallback-iem (rzadko wykorzystywany). Wymaga licencji Active Data Guard. |

## Pliki przeniesione do `sql/legacy_19c/`

- `validate_environment.sql`
- `tac_full_readiness.sql`
- `tac_replay_monitor.sql`
- `fsfo_check_readiness.sql`
- `fsfo_monitor.sql`

---

## Compliance score po wdrożeniu (oszacowane)

| Obszar | Przed (z PLAN) | Po wdrożeniu |
|--------|----------------|--------------|
| **A. MAA / RAC** | PASS | PASS |
| **B. Wersje binariów** | PASS | PASS |
| **C. iSCSI LVM** | PASS | PASS |
| **D. DG Broker + FSFO Master** | PASS | PASS |
| **D'. FSFO Backup Observers** | FAIL | **PASS** ✅ |
| **E. TAC service config** | PARTIAL | **PASS** ✅ (F-02/06/15/25) |
| **F. Wallet auto-login Observer** | PASS | PASS |
| **G. systemd Observer daemon** | PASS | PASS |
| **H. Skrypty automatyzacji** | PARTIAL | **PASS** ✅ (F-01/04/12/19/20) |
| **I. Walidacja SQL pre/post-deploy** | PARTIAL | **PASS** ✅ (F-02/05/06/11) |
| **J. Klient TAC (UCP/JDBC)** | PARTIAL | **PASS** ✅ (F-09 + pom.xml) |
| **K. Dokumentacja end-to-end** | PARTIAL | **PASS** ✅ (F-08/10/13/18.F/21/22) |
| **L. Bezpieczeństwo** | FAIL | **PASS** ✅ (F-04 wallet + .lab_secrets) |
| **M. Sieć/Firewall** | PARTIAL | **PASS** ✅ (F-07) |
| **NEW. Wydajność** | — | **PASS** ✅ (F-18 pełen pakiet) |

**Score po wdrożeniu:** 14 / 14 PASS dla zakresu P0+P1 (≈ 100 % zgodności blokerów go-live).

---

## Kolejny krok (po sesji 2026-04-27/28)

1. Stworzyć `/root/.lab_secrets` na **wszystkich** VM (chmod 600) z `export LAB_PASS='Oracle26ai_LAB!'` przed uruchomieniem skryptów `ssh_setup.sh`, `setup_observer.sh`, `create_standby_broker.sh`.
2. Pełny dry-run instalacji od kickstartów (F-18.C → 02_OS_and_Network_Preparation_PL → ... → 09_Test_Scenarios_PL).
3. Po DBCA: pomiar wydajności (`docs/10_Performance_Tuning.md` sekcja 4) — DBCA czas, fio IOPS, time drift count.

---

## Sesja 2026-04-29 — pierwsza realna instalacja (kroki 2–6)

**Kontekst:** VM-ki zainstalowane ze starych kickstartów (bez DNS/NTP/sshpass). Instalacja ręczna przez MobaXterm — pliki kopiowane do `/tmp/` na każdym serwerze.

### Poprawki błędów odkrytych podczas instalacji

| ID | Plik | Problem | Naprawa |
|----|------|---------|---------|
| **S29-01** | `scripts/ssh_setup.sh` | `sshpass` instalowany tylko lokalnie (prim01); przy "Kopiowanie klucza z prim02" brak sshpass na prim02 → `command not found` | Dodano pętlę instalującą sshpass na wszystkich węzłach (`ALL_NODES`) przed key distribution |
| **S29-02** | `kickstart/ks-*.cfg` (5 plików) | `sshpass` niedostępny po kickstart → ręczna instalacja wymagana | Dodano `sshpass` do `%packages` we wszystkich 5 kickstartach |
| **S29-03** | `scripts/validate_env.sh` | SSH test (sekcja 3) wykonywany z roota bez `su - oracle` → zawsze WARN mimo poprawnych kluczy | Zmieniono na `su - oracle -c "ssh -o BatchMode=yes ..."` |
| **S29-04** | `scripts/validate_env.sh` | ASM disk check: ścieżka `/dev/oracleasm/disks` (nieistniejąca) zamiast `/dev/oracleasm/` | Poprawiono ścieżkę; grep iSCSI rozszerzony: `scsi-\|iscsi` |
| **S29-05** | `scripts/validate_env.sh` | Komunikat memlock odwoływał się do `99-oracle-memlock.conf` (stara nazwa) | Zmieniono na `zz-oracle-memlock.conf` |
| **S29-06** | `docs/*.md` + `README.md` | `<repo>/` w komendach nieczytelne w praktyce — użytkownik kopiuje pliki do `/tmp/` przez MobaXterm | Bulk replace `<repo>/` → `/tmp/` (50 wystąpień w 11 plikach); README "Notacja" przepisana (workflow MobaXterm/scp → `/tmp/`) |
| **S29-07** | `docs/*.md` (9 plików) | PowerShell 5.1 `Get-Content` bez `-Encoding` czyta UTF-8 bez BOM jako CP1250; `Set-Content -Encoding utf8` zapisuje z BOM → polskie znaki zniszczone | Odwrócona konwersja przez `[System.IO.File]::ReadAllBytes` + decode UTF-8 + encode CP1250 + decode UTF-8 → write bez BOM; skrypt poprawiony do używania `WriteAllBytes` |

### Stan po sesji 2026-04-29

| Krok | Status |
|------|--------|
| DNS (bind9 na infra01) | ✅ działa — `dig scan-prim.lab.local` zwraca .31/.32/.33 |
| NTP (chrony) | ✅ zsynchronizowany (Stratum > 0) |
| SSH Full Mesh (grid + oracle) | ✅ 16/16 SUCCESS (prim01/prim02/stby01/infra01) |
| iSCSI Target (infra01) | ✅ skonfigurowany |
| iSCSI Initiator (prim01, prim02) | ✅ zalogowany; ASM disks: DATA1/RECO1/OCR1/OCR2/OCR3 |
| validate_env.sh | ✅ **16 PASS, 3 WARN** (porty — OK pre-install), **0 FAIL** |
| Grid Infrastructure install | 🔄 W TOKU — `install_grid_silent.sh` uruchomiony na prim01 (grid user) |

### Następny krok

Po zakończeniu `install_grid_silent.sh`:
1. `orainstRoot.sh` jako root na prim01
2. `root.sh` jako root na prim01 — poczekaj
3. `root.sh` jako root na prim02 (sekwencyjnie!)
4. Powrót do instalatora (Enter)
5. `crsctl stat res -t` — weryfikacja CRS ONLINE
4. Po wdrożeniu Backup Observerów: scenariusz 5 z `docs/09_Test_Scenarios_PL.md` (awaria Master).
5. (Decyzja zespołu) — kiedy podnieść `compatible=23.4.0` → `23.26.0`.

---

**Autor wdrożenia:** Claude Opus 4.7 (1M context) — auto-mode | **Data:** 2026-04-27

---

## Sesja 2026-04-28 — znaleziska i poprawki pre-launch

| ID | Status | Pliki | Opis |
|----|--------|-------|------|
| **S28-01** | ✅ DONE | `docs/02_OS_and_Network_Preparation_PL.md` (nowa sekcja 5) | **Shared folders / Guest Additions** — Wyjaśniono że na OL 8.10 z UEK moduł `vboxsf.ko` jest wbudowany w `kernel-uek-modules-extra` (Oracle rozwija jednocześnie OL i VirtualBox). Nie trzeba instalować Guest Additions ISO. Na RHEL/CentOS/AlmaLinux/Rocky/OL z RHCK — wymagana instalacja GA z ISO (instrukcja dodana). Dodano też opis race condition przy boot (`nofail` + ręczny `mount`). |
| **S28-02** | ✅ DONE | `scripts/vbox_create_vms.ps1` | **Zły katalog docelowy VM** — `$VmRoot` był zahardkodowany jako `"D:\VirtualBox VMs"`. VirtualBox ma skonfigurowany default machine folder jako `D:\VM` (wszystkie inne VM-ki użytkownika tam są). Zmieniono na `"D:\VM"`. |
| **S28-03** | ✅ DONE | `FIXES_PLAN_v2_PL.md`, `docs/02_OS_and_Network_Preparation_PL.md` | **Pełna ścieżka w docs** — `D:\__AI__\_oracle_\20260423-FSFO-TAC-guide\VMs2-install` pojawiała się dosłownie w dwóch plikach zamiast oznaczenia `<repo>`. Zastąpiono. `README.md` (definicja `<repo>`) pozostał bez zmian. |
| **S28-04** | ✅ DONE | `docs/02_OS_and_Network_Preparation_PL.md` (nowa sekcja 1, renumeracja) | **Brak dokumentacji kroku "tworzenie VM"** — `vbox_create_vms.ps1` był wspomniany tylko przy okazji shared folders, ale sam krok tworzenia definicji VM w VirtualBox nie był nigdzie opisany w docs. Dodano sekcję 1 ("Tworzenie maszyn wirtualnych w VirtualBox") z tabelą wymagań, opisem działania skryptu i tabelą 5 VM. Stare sekcje 1–4 → 2–5. |
| **S28-05** | ✅ DONE | `scripts/vbox_create_vms.ps1` linia 98 | **Bug PowerShell: `$name:` → `${name}:`** — PowerShell w stringu `"$name: --hostiocache off..."` interpretuje `$name:` jako odwołanie do PSDrive (analogia do `$env:`, `$HKLM:`), co powoduje `ParseException: InvalidVariableReferenceWithDrive`. Poprawiono na `${name}:`. W `VMs/` skrypt nie miał tego wzorca w tym miejscu, stąd tam działało. |
| **S28-06** | ✅ DONE | `scripts/vbox_create_vms.ps1` sekcja PODSUMOWANIE + sekcja shared folder | **Dwa bugi podczas pierwszego uruchomienia skryptu:** (1) `Measure-Object -Property RamMB` nie działa na tablicy hashtable `@{}`  — NativeCommandError + wcześniejszy exit. Poprawiono na `foreach` loop z `+=`. (2) `sharedfolder add` nie był idempotentny — przy drugim uruchomieniu rzucał `already exists` NativeCommandError, który przy `$ErrorActionPreference="Stop"` przerywał cały skrypt. Poprawiono: `showvminfo --machinereadable` sprawdza czy shared folder istnieje przed `add`. |

**Autor:** Claude Sonnet 4.6 | **Data:** 2026-04-28

---

## Sesja 2026-04-28 (część 3) — stby01 GI + GI RAC fixes ✅ DONE

| ID | Status | Pliki | Opis |
|----|--------|-------|------|
| **S28-07** | ✅ DONE | `scripts/setup_oracle_env.sh` v1.2, `docs/02b_OS_Preparation_Manual_PL.md` (sekcje 2.14/3.1/4.13), `docs/04_Grid_Infrastructure.md` (hint weryfikacja) | **ORA-12162 `sqlplus / as sysasm`** — brak `ORACLE_SID` w profilu użytkownika `grid` po uruchomieniu `setup_oracle_env.sh`. Odkrycie: `ORACLE_SID` per-węzeł różne (prim01=`+ASM1`, prim02=`+ASM2`, stby01=`+ASM`). Fix: dodano opcjonalny 5. parametr `OSID` do `setup_profile()` w skrypcie; osobny idempotentny blok SID; per-węzeł wywołania dla grid zamiast pętli. Docs uzupełnione o `export ORACLE_SID` w profilach + hint `export ORACLE_SID=+ASM1` przy weryfikacji. |
| **S28-08** | ✅ DONE | `docs/04_Grid_Infrastructure.md` (nowa sekcja 2a w Metodzie 1 i 2) | **+DATA/+RECO OFFLINE na prim02** — `asmca -silent -createDiskGroup` tworzy diskgroup tylko na lokalnej instancji ASM; prim02 musi ją oddzielnie zamontować (`srvctl start diskgroup -g DATA -n prim02`). Wcześniej brakowało sekcji tworzenia +DATA/+RECO w dokumentacji — DBCA zakłada ich istnienie, ale żaden skrypt/RSP ich nie tworzył. Dodano sekcję 2a (obie metody) z `asmca` komendami + weryfikacją `asmcmd lsdg`. |
| **S28-09** | ✅ DONE | `response_files/grid_restart.rsp` v3.0, `docs/04_Grid_Infrastructure.md` (sekcja 3 obie metody) | **INS-30507 stby01 — Oracle GI 23.26.1 ignoruje `FILE_SYSTEM_STORAGE` w `HA_CONFIG`** — Installer zawsze wymaga dysków ASM niezależnie od `storageOption`. Sprawdzone: poprawny RSP, log pusty (brak linii z błędem), template potwierdza `HA_SWONLY` = tylko Windows. Fix: `installOption=CRS_SWONLY` — pomija walidację storage; `root.sh` samodzielnie konfiguruje OHAS + lokalny CSS z plikowym OCR. Brak `executeConfigTools` dla `CRS_SWONLY`. RSP v3.0: usunięto storageOption/ocrLocations/votingFilesLocations/sysasmPassword/scanType/clusterName/networkInterfaceList. Dodano dwujęzyczny komentarz wyjaśniający decyzję. |
| **S28-10** | ✅ DONE | `kickstart/ks-stby01.cfg` (już zawierał, weryfikacja), ręczny fix na VM | **INS-32031/32033 stby01 — brak `/u01/app/oraInventory` i `/etc/oraInst.loc`** — VM zainstalowana ze starym kickstartem (przed poprawkami z sesji S28). Aktualny `ks-stby01.cfg` już ma `mkdir -p /u01/app/oraInventory` + tworzenie `oraInst.loc`. Na bieżącej VM: ręczny `mkdir -p /u01/app/oraInventory && chown grid:oinstall /u01/app/oraInventory && chmod 770 /u01/app/oraInventory` + `cat > /etc/oraInst.loc` jako root. |
| **S28-11** | ✅ DONE | `scripts/install_grid_silent.sh` (komunikat końcowy) | **Mylący komunikat po instalacji** — skrypt sugerował "dla klastra RAC - najpierw prim01, potem prim02" bez rozróżnienia na Oracle Restart. Poprawiono: komunikat zawiera teraz pełne komendy (`orainstRoot.sh` + `root.sh`) oraz osobną notę dla RAC i Oracle Restart (stby01 — brak executeConfigTools). |
| **S28-12** | ✅ DONE | `docs/04_Grid_Infrastructure.md` (sekcja 3 obie metody) | **`root.sh` dla CRS_SWONLY nie konfiguruje OHAS** — wbrew wcześniejszemu założeniu, `root.sh` dla `CRS_SWONLY` wykonuje tylko bazowe OS setup (oraenv, /etc/oratab). OHAS nie jest uruchomiony. `-executeConfigTools` z `HA_CONFIG` RSP oczekuje root.sh w kontekście `HA_CONFIG` i kończy błędem "privileged user configuration not completed". Fix: `roothas.pl` bezpośrednio jako root — `CLSRSC-327: Successfully configured Oracle Restart for a standalone server`. Docs zaktualizowane: krok 3 stby01 (obie metody) zawiera teraz `roothas.pl` po `root.sh`. |

**Autor:** Claude Sonnet 4.6 | **Data:** 2026-04-28

---

## Sesja 2026-04-28 (część 4) — DB Software install (prim01/prim02) ✅ DONE

| ID | Status | Pliki | Opis |
|----|--------|-------|------|
| **S28-13** | ✅ DONE | `response_files/db.rsp` | **INS-10105 — nieważny RSP dla 23.26.1** — `oracle.install.db.runCVUChecks` nie istnieje w schemacie 23.26.1 (stary parametr); `oracle.install.db.rootconfig.sudoPassword` — również nieobecny; `oracle.install.db.clusterNodes` → `oracle.install.db.CLUSTER_NODES` (uppercase). Usunięto dwa nieważne parametry, zmieniono nazwę clusterNodes. DB software zainstalowane na prim01+prim02, root.sh wykonane na obu węzłach. |

**Autor:** Claude Sonnet 4.6 | **Data:** 2026-04-28

---

## Sesja 2026-04-28 (część 5) — DBCA DONE + poprawki dokumentacji ✅

| ID | Status | Pliki | Opis |
|----|--------|-------|------|
| **S28-14** | ✅ DONE | `response_files/grid_restart.rsp` v3.0 | **Błędny komentarz** — poprzedni tekst twierdził że `root.sh` samodzielnie konfiguruje OHAS. W rzeczywistości dla `CRS_SWONLY` `root.sh` wykonuje tylko bazowe OS setup; OHAS konfiguruje dopiero `roothas.pl`. Komentarz poprawiony — opisuje 2-krokową procedurę: `root.sh` (OS only) → `roothas.pl` (OHAS). |
| **S28-15** | ✅ DONE | `response_files/grid_restart_config.rsp` | **Brak ostrzeżenia o podejściu niedziałającym** — plik dokumentował metodę `executeConfigTools + HA_CONFIG`, która w 23.26.1 kończy się błędem, bez żadnego ostrzeżenia. Dodano nagłówek `!! NIE UŻYWAĆ !!` z historią błędu i wskazaniem poprawnej metody (`roothas.pl`). |
| **S28-16** | ✅ DONE | `docs/05_Database_Primary.md` | **Brak logów DBCA w instrukcji + brak nohup dla DBCA** — dodano sekcję "3. Logi DBCA — gdzie szukać" z tabelą 3 lokalizacji logów; Metoda 1 zmieniona z czystego `bash` na `nohup ... &` z `echo PID` i `tail -f`; dodano uwagę o ryzyku zerwanej sesji SSH i potrzebie `dbca -deleteDatabase` po przerwaniu. |
| **S28-17** | ✅ DONE | `docs/06_Data_Guard_Standby.md` | **Brak ostrzeżenia nohup/screen dla DGMGRL** — w Metodzie 2 Krok 3 dodano block-quote z ostrzeżeniem o ryzyku zerwanej sesji SSH przy `CREATE PHYSICAL STANDBY DATABASE` oraz przykładem użycia `screen`/`tmux`. |
| **S28-18** | ✅ DONE | `docs/06_Data_Guard_Standby.md` (nowa sekcja 0) | **Brak kroku instalacji DB Software na stby01** — Metoda 1 skakała wprost do `create_standby_broker.sh` bez instalacji binarek, które są warunkiem koniecznym (katalog `dbs/` musi istnieć). Dodano sekcję "0. Wymaganie wstępne: Instalacja oprogramowania DB na stby01" z metodą automatyczną, manualną i weryfikacją. |
| **S28-19** | ✅ DONE | `response_files/db_stby.rsp` (nowy plik) | **Brak osobnego RSP dla stby01** — `db.rsp` ma `CLUSTER_NODES=prim01,prim02`; użyty na standalone stby01 spowodowałby próbę rejestracji home w RAC CRS → błąd. Stworzono `db_stby.rsp` jako kopię `db.rsp` z pustym `CLUSTER_NODES=` i komentarzem wyjaśniającym różnicę. |
| **S28-20** | ✅ DONE | `docs/05_Database_Primary.md`, `docs/06_Data_Guard_Standby.md` | **Zbędne kopiowanie `cp /tmp/... /home/oracle/`** — skrypty i pliki RSP uruchamiane bezpośrednio z `/tmp/scripts/` i `/tmp/response_files/` (gdzie kopiuje je MobaXterm). Usunięto wszystkie `cp` z obu dokumentów. |
| **DBCA** | ✅ DONE | — | Baza `PRIM` (CDB + PDB `APPPDB`) utworzona pomyślnie. `LOG_MODE=ARCHIVELOG`, `FLASHBACK_ON=YES`, `FORCE_LOGGING=YES`. |

**Autor:** Claude Sonnet 4.6 | **Data:** 2026-04-28

---

## Sesja 2026-04-28 (część 6) — DB Software install stby01: fix grup OS

| ID | Status | Pliki | Opis |
|----|--------|-------|------|
| **S28-22** | ✅ DONE | `scripts/create_standby_broker.sh`, `scripts/setup_observer.sh` | **mkstore `-p` + `-autoLogin` — obie flagi niepoprawne w 23.26.1** — `mkstore --help` potwierdza brak `-autoLogin` (dostępne: `-createSSO`, `-createLSSO`, `-createALO`). Dodatkowo: `-p PASSWORD` odrzucane przez mkstore 23.26.1 → `MaxRetryException: Exceeded maximum allowed password retry limit` już przy pierwszym wywołaniu (`-create`). Stary VMs skrypt (v1.3, linia 116) wskazuje poprawną metodę: hasło przez stdin (heredoc). Fix w `create_standby_broker.sh`: wszystkie 3 wywołania mkstore zmienione na heredoc + `-autoLogin` → `-createSSO`. Fix w `setup_observer.sh`: `-autoLogin` → `-createSSO` (ten skrypt już używał heredoc dla `-create`/`-createCredential`). |
| **S28-21** | ✅ DONE | `kickstart/ks-stby01.cfg` | **INS-35341 stby01 — oracle nie jest w grupie `racdba`** — `db_stby.rsp` deklaruje `OSRACDBA_GROUP=racdba`, ale `ks-stby01.cfg` nie zawierał `groupadd racdba` ani nie dodawał oracle/grid do tej grupy. Porównanie z `ks-prim01.cfg` (który ma `groupadd -g 54330 racdba` + oracle z `racdba,asmadmin,asmoper`) ujawniło też brak `asmadmin,asmoper` dla oracle i brak `racdba` dla grid. Naprawiono 3 linie: dodano `groupadd -g 54330 racdba`; `useradd grid` → dodano `racdba`; `useradd oracle` → dodano `racdba,asmadmin,asmoper` (zgodnie z prim01). Bieżąca VM: ręczny fix `groupadd racdba && usermod -aG racdba,asmadmin,asmoper oracle && usermod -aG racdba grid` jako root, nowa sesja SSH oracle. |

**Autor:** Claude Sonnet 4.6 | **Data:** 2026-04-28

---

## Sesja 2026-04-28 (część 7) — Data Guard Standby (create_standby_broker.sh) 🔄 W TOKU

| ID | Status | Pliki | Opis |
|----|--------|-------|------|
| **S28-23** | ✅ DONE | `scripts/create_standby_broker.sh` | **Krok 0 wallet — poprawka mkstore działa** — Po poprawce FIX-097/S28-22 (heredoc + `-createSSO`) krok tworzenia lokalnego wallet DGMGRL przeszedł bez błędów. |
| **S28-24** | ✅ DONE | `scripts/create_standby_broker.sh` | **`asmcmd pwget` — exit 2 w kroku 1 (password file)** — `asmcmd pwget --dbuniquename PRIM` uruchamiane jako oracle z DB home nie może połączyć się z `+ASM1` (wymaga środowiska GI: grid user + GI_HOME) → exit 2 → `set -euo pipefail` killuje skrypt. Fix: zastąpiono `asmcmd pwget/pwcopy` przez `orapwd file=/tmp/pwd/orapwPRIM password="$LAB_PASS" dbuniquename=PRIM force=y`. W labie SYS=LAB_PASS więc nowy plik semantycznie identyczny z tym w ASM. |
| **S28-25** | ✅ DONE | `scripts/create_standby_broker.sh` | **`. oraenv` exit 2 — PRIM1 nie w /etc/oratab** — `bash -x` ujawnił: `dbhome PRIM1` zwraca `/home/oracle` (PRIM1 brak w /etc/oratab; RAC CRS zarządza instancjami). Oraenv ustawiał `ORACLE_HOME=/home/oracle` i kończył exit 2 → `set -e` killował skrypt. Fix: usunięto `. oraenv` z kroku 1; ORACLE_HOME i PATH są już poprawnie ustawione z oracle `.bash_profile`. |
| **S28-26** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **`orapwd dbuniquename` — OPW-00008 exit 255** — parametr `dbuniquename=PRIM` w `orapwd` działa tylko gdy plik docelowy jest w ASM (nie w filesystemie). Dla `/tmp/pwd/orapwPRIM` (filesystem) jest niedozwolony → OPW-00008 → exit 255 → `set -e` killował skrypt. Fix: usunięto `dbuniquename=PRIM` z wywołania orapwd. |
| **S28-27** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **`orapwd` OPW-00029 — hasło zawiera "oracle"** — orapwd 23.26.1 odrzuca hasła zawierające słowo "oracle" (case-insensitive). `Oracle26ai_LAB!` (15 znaków) to triggeruje. Potwierdzone testem: `orapwd file=/tmp/test.pwd password="$LAB_PASS" force=y` → FAIL. Fix: rezygnacja z orapwd; powrót do `asmcmd pwget/pwcopy` z `ORACLE_SID=+ASM1` (łączy z instancją ASM bezpośrednio, bez oraenv). |
| **S28-28** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **`asmcmd pwcopy` ORA-27040 Permission denied** — `asmcmd pwcopy` wykonuje operację przez proces ASM (grid user). Katalog `/tmp/pwd` tworzony przez oracle z domyślnymi uprawnieniami (755, owner: oracle) blokuje zapis przez grid → ORA-19505/ORA-27040. Fix: `chmod 1777 /tmp/pwd` (world-writable + sticky) przed pwcopy; usunięto `chmod 640` (oracle nie może zmieniać uprawnień pliku należącego do grid, ale czyta go przez grupę oinstall). |
| **S28-29** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **ORA-12514 DGMGRL — SERVICE_NAME bez domeny** — Oracle 23.26.1 z `db_domain=lab.local` rejestruje serwisy jako `PRIM.lab.local` (nie `PRIM`). tnsnames z `SERVICE_NAME=PRIM` → listener nie matchuje → ORA-12514. Znane z VMs/FIXES_LOG FIX-040. Fix: `SERVICE_NAME=PRIM.lab.local` i `STBY.lab.local` w tnsnames na prim01/prim02 i stby01; `GLOBAL_DBNAME=STBY.lab.local`/`STBY_DGMGRL.lab.local` w listener.ora stby01. Dodano też credential STBY do wallet (wymagany dla sqlplus /@STBY w kroku 7). |
| **S28-30** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **ORA-01081 — baza STBY już uruchomiona w kroku 3** — Oracle Restart na stby01 automatycznie uruchamia bazę STBY po instalacji oprogramowania DB. Krok 3 wywoływał `STARTUP NOMOUNT` bez wcześniejszego zatrzymania → ORA-01081. Błąd nie zatrzymywał skryptu (exit code SSH=0 mimo błędu sqlplus), więc baza pozostawała w OPEN/MOUNT zamiast NOMOUNT → CREATE PHYSICAL STANDBY zawiódłby. Fix: dodano `srvctl stop database -db STBY 2>/dev/null || true` i `SHUTDOWN ABORT;` przed `STARTUP NOMOUNT`. |
| **S28-31** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **ORA-12514 DGMGRL — SCAN listener nie widzi PRIM.lab.local** — tnsnames używał `HOST=scan-prim.lab.local`. SCAN listener (192.168.56.31-33) nie ma serwisu `PRIM.lab.local` bo `remote_listener` nie jest ustawiony w bazie PRIM (PMON rejestruje tylko do lokalnych listenerów). Potwierdzone: `lsnrctl status` na prim01 pokazuje `PRIM.lab.local` na listenerze lokalnym; `lsnrctl status LISTENER_SCAN1` → TNS-01101. Fix: zmieniono `ADDRESS` na `ADDRESS_LIST` z `prim01.lab.local:1521` i `prim02.lab.local:1521` (node IPs) — lokalne listenery na obu node mają serwis. |
| **S28-32** | ✅ DONE | `scripts/create_standby_broker.sh` | **Prewencyjny FIX-042: reset remote_listener na STBY po CREATE PHYSICAL STANDBY** — po stworzeniu standby STBY dziedziczy SPFILE z RAC primary, który ma `remote_listener=scan-prim.lab.local:1521`. stby01 to Single Instance (Oracle Restart) bez SCAN — `remote_listener` musi być zresetowany, inaczej PMON stby01 próbuje rejestrować do SCAN VIP (nieosiągalnego z SI). Znane z VMs/FIXES_LOG FIX-042. Dodano krok 6b w skrypcie: `ALTER SYSTEM RESET remote_listener` + `SET local_listener` + `ALTER SYSTEM REGISTER`. |
| **S28-33** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **FIX-061 + FIX-075: DGMGRL 26ai — MAINTAINED AS PHYSICAL usunięte, CREATE PHYSICAL STANDBY nie istnieje** — `ADD DATABASE STBY ... MAINTAINED AS PHYSICAL` → "Syntax error before MAINTAINED" (usunięte w 26ai). `CREATE PHYSICAL STANDBY DATABASE` → "Syntax error before PHYSICAL" (nigdy nie istniało jako komenda DGMGRL). Potwierdzone przez `help create` — tylko `CREATE CONFIGURATION` i `CREATE FAR_SYNC`. Fix: nowy przepływ 3-fazowy: (1) DGMGRL CREATE CONFIGURATION + ADD DATABASE (bez MAINTAINED), (2) RMAN DUPLICATE TARGET DATABASE FOR STANDBY FROM ACTIVE DATABASE z SPFILE SET, (3) DGMGRL EDIT (StaticConnectIdentifier) + ENABLE CONFIGURATION. FIX-043: RMAN auxiliary przez SID=STBY + UR=A (NOMOUNT mode, db_domain nieaktywna). |
| **S28-34** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **ORA-00439: feature not enabled: Real Application Clusters** — RMAN DUPLICATE kopiuje SPFILE z RAC primary (cluster_database=TRUE). Na SI standby (stby01) Oracle RAC nie jest dostępne → ORA-00439 przy STARTUP MOUNT. Znane z VMs/FIXES_LOG FIX-041. Fix: dodano `SET cluster_database = 'FALSE'` do RMAN DUPLICATE SPFILE klauzuli. Post-RMAN (krok 6b): `ALTER SYSTEM SET cluster_database_instances=1` + `instance_number=1 SCOPE=SPFILE` (nie można ich ustawiać w RMAN SET — RMAN-06581 w 26ai). |
| **S28-35** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **ORA-16525: broker not yet available** — CREATE CONFIGURATION wywołane tuż po `ALTER SYSTEM SET DG_BROKER_START=TRUE`. Broker startuje asynchronicznie i potrzebuje ~20-30s. Fix: przeniesiono CREATE CONFIGURATION + ADD DATABASE z kroku 6 (przed RMAN) do kroku 6c (po RMAN Duplicate — broker na pewno gotowy po kilku minutach RMAN). Krok 6 zastąpiony `sleep 30`. |
| **S28-36** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **ORA-27106: system pages not available to allocate memory** (additional info: 6508, 2, 3) — RMAN DUPLICATE kopiuje SPFILE z RAC primary który ma `use_large_pages=ONLY` (ustawione przez F-18.B w `dbca_prim.rsp`). stby01 nie ma skonfigurowanych HugePages (F-18.A dotyczy tylko prim01/prim02 przez kickstart). Po restarcie auxiliary z nowym SPFILE Oracle próbuje zarezerwować HugePages → brak → ORA-27106. Potwierdzone: `grep HugePages /proc/meminfo` na stby01 → `HugePages_Total: 0`. Fix: dodano `SET use_large_pages = 'FALSE'` do RMAN DUPLICATE SPFILE klauzuli. |
| **S28-37** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **ORA-19660 / ORA-19685 / ORA-19845 / ORA-17628 / ORA-01138** podczas RMAN DUPLICATE — primary RAC używa OMF (`db_create_file_dest='+DATA'`, `db_recovery_file_dest='+RECO'`). Klon dziedziczy te parametry przez SPFILE. stby01 (XFS, bez ASM) → przy verify backupset Oracle próbuje pisać do +DATA → "files in backup set could not be verified", "SPFILE could not be verified". Znane z VMs/FIXES_LOG **FIX-044**. Fix: dodano `SET db_create_file_dest='/u02/oradata/STBY'`, `SET db_recovery_file_dest='/u03/fra/STBY'`, `SET db_recovery_file_dest_size='14G'`, `SET control_files='/u02/oradata/STBY/control01.ctl','/u03/fra/STBY/control02.ctl'` do RMAN SPFILE klauzuli. Dodatkowo: krok 3 (NOMOUNT) czyści `/u02/oradata/STBY/*.dbf|*.ctl`, `/u03/fra/STBY/*.ctl`, `onlinelog/*`, `$ORACLE_HOME/dbs/spfileSTBY.ora` z poprzednich prób. |
| **S28-38** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **ORA-01138 z TARGET podczas DBMS_BACKUP_RESTORE.RESTORESETPIECE** (mimo że oba PRIM1+PRIM2 są OPEN — potwierdzone `srvctl status database -db PRIM`) — tnsnames alias PRIM miał `LOAD_BALANCE=on`, RMAN target i AUX RPC back trafiały na różne nody RAC w różnych krokach Active Duplicate. DBMS_BACKUP_RESTORE wymaga koherentnego state PRIM1+PRIM2; cross-instance RPC podczas RESTORESETPIECE → "database must either be open in this instance or not at all". Wzorzec analogiczny do VMs/FIXES_LOG **FIX-085** (LOAD_BALANCE=ON nondeterministic). Fix: zmieniono na `LOAD_BALANCE=off` + `FAILOVER=on` w obu blokach tnsnames (na stby01 — krok 2; na prim01+prim02 — krok 4). Wszystkie połączenia teraz pinują do prim01, z fallback na prim02 jeśli prim01 down. |
| **S28-39** | ⚠️ NOTE | (state issue, nie skrypt) | **PRIM2 zatrzymał się w `Mounted (Closed)` po wcześniejszych nieudanych próbach RMAN** — `crsctl stat res ora.prim.db -t` pokazywał PRIM1 OPEN ale PRIM2 INTERMEDIATE/Mounted, APPPDB OFFLINE na PRIM2. To było faktycznym root cause ORA-01138 (mimo że LOAD_BALANCE=off i tak był potrzebny). Fix: `srvctl stop instance -db PRIM -instance PRIM2 -force` + `srvctl start instance -db PRIM -instance PRIM2 -startoption OPEN` + `ALTER PLUGGABLE DATABASE APPPDB OPEN INSTANCES=ALL` + `SAVE STATE INSTANCES=ALL`. Sanity check: zawsze `srvctl status database -db PRIM -verbose` przed RMAN — oba "Open". |
| **S28-40** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **ORA-16532 w SHOW CONFIGURATION na PRIM po ENABLE CONFIGURATION** — `dg_broker_config_file1/2` domyślnie wskazują na lokalny `$ORACLE_HOME/dbs/dr1PRIM.dat` i `dr2PRIM.dat`. W RAC PRIM1 zapisuje plik konfig brokera lokalnie, ale PRIM2 widzi pusty katalog → ORA-16532 "broker configuration does not exist". Fix: w kroku 5 (przed pierwszym `DG_BROKER_START=TRUE`) dodano `ALTER SYSTEM SET dg_broker_config_file1='+DATA/PRIM/dr1PRIM.dat' SCOPE=BOTH SID='*'` i `dg_broker_config_file2='+RECO/PRIM/dr2PRIM.dat'`. Broker startując utworzy pliki bezpośrednio w shared storage ASM — oba nody RAC widzą tę samą konfigurację. UWAGA: ORA-16573 blokuje zmianę przy enabled brokerze → trzeba REMOVE CONFIGURATION + DG_BROKER_START=FALSE → zmiana → DG_BROKER_START=TRUE → CREATE CONFIGURATION. Skrypt teraz robi to OD RAZU w kroku 5 — bez "tańców". |
| **S28-41** | ✅ DONE | `scripts/setup_oracle_env.sh` | **ORA-12162 "net service name is incorrectly specified" przy `sqlplus / as sysdba` na prim02 i stby01** — `setup_oracle_env.sh` v1.1 ustawiał `ORACLE_SID` w `.bash_profile` tylko dla użytkownika `grid` (per-wezel: +ASM1/+ASM2/+ASM), ale nie dla `oracle`. Bez ORACLE_SID sqlplus szuka aliasu sieciowego z pustego stringa → ORA-12162. Fix v1.2: setup_profile dla `oracle` per-host: prim01→PRIM1, prim02→PRIM2, stby01→STBY. Idempotentny — re-uruchomienie skryptu dopisze tylko brakujące ORACLE_SID na ww. nodach. |
| **S28-42** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **ORA-16603 "member is part of another Oracle Data Guard broker configuration" przy `ADD DATABASE STBY` z PRIM** — mimo że PRIM miał świeżą konfigurację (po REMOVE+CREATE), STBY nadal pamiętał starą konfigurację z lokalnych plików `$ORACLE_HOME/dbs/dr1STBY.dat` i `dr2STBY.dat` (utworzone wcześniej gdy `DG_BROKER_START=TRUE` ustawione w SPFILE z FIX-S28 SET clause RMAN DUPLICATE). Broker DMON na STBY przy starcie odczytał stare pliki → STBY twierdzi "I'm in fsfo_cfg" → ORA-16603 przy ADD. Fix: w kroku 3 (cleanup poprzednich prób, FIX-S28-37) dodano `rm -f $ORACLE_HOME/dbs/dr1STBY.dat $ORACLE_HOME/dbs/dr2STBY.dat` — pliki będą tworzone od zera przy nowym ENABLE. Manual recovery: `ALTER SYSTEM SET DG_BROKER_START=FALSE` na STBY → `rm dr*STBY.dat` → `ALTER SYSTEM SET DG_BROKER_START=TRUE`. |
| **S28-43** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **Apply Lag rośnie liniowo + ORA-16809/16853/16855/16857 mimo poprawnej konfiguracji DG** — bez Standby Redo Logs (SRL) na STBY broker shippuje redo tylko przez archiwizowane logi (nie real-time). W idle labie nowy archived log powstaje dopiero przy log switch — Apply Lag = (now − czas ostatniego archived log na PRIM). Plus FSFO wymaga SRL żeby ENABLE FAST_START FAILOVER zadziałało. Fix: w skrypcie dodano dwa nowe kroki: (1) krok 5 (po DG_BROKER_START=TRUE) tworzy 6 SRL na PRIM w +DATA (3 per thread × 2 thready RAC, idempotentne — sprawdza COUNT(*)); (2) nowy krok 6c (między post-RMAN ALTER SYSTEM a DGMGRL CREATE CONFIGURATION) tworzy 6 SRL na STBY w `/u02/oradata/STBY/onlinelog/srl_t<N>g<N>.log` (XFS, bo stby01 bez ASM). DGMGRL ENABLE renumerowane na krok 6d. Po ENABLE: Lag = 0s, Real Time Query = ON, Configuration Status = SUCCESS bez warningów. UWAGA: ORA-01156 ("recovery in progress") gdy próbujesz ADD STANDBY LOGFILE z aktywnym MRP — zawsze APPLY-OFF przed dodawaniem na działającym DG. W skrypcie SRL tworzone PRZED ENABLE CONFIGURATION (gdy MRP nie działa). Plus mini-fix S28-41-bis: `cluster_database_instances` w SI rzuca ORA-02065 → wrapped w `WHENEVER SQLERROR CONTINUE` (instance_number wystarczy). |
| **S28-44** | ✅ DONE | `scripts/create_standby_broker.sh` | **Idempotency + sanity check dla bezpiecznego re-runa skryptu**: (a) **0a — Pre-flight stan PRIM**: `srvctl status database -db PRIM -verbose` → grep `Instance status: Open`, oczekuje 2 wystąpień. Jeśli mniej (np. PRIM2 w `Mounted (Closed)` po wcześniejszym crash — lekcja S28-39) → exit 1 z instrukcją recovery. Defensywnie zapobiega traceniu 30 min na RMAN który zwróci ORA-01138. (b) **0b — Defensive broker reset na PRIM**: `ALTER SYSTEM SET DG_BROKER_START=FALSE SCOPE=BOTH SID='*'` z `WHENEVER SQLERROR CONTINUE` — greenfield no-op, re-run zatrzymuje brokera tak żeby krok 5 mógł zmienić `dg_broker_config_file1/2` bez ORA-16573 ("attempt to change configuration file for an enabled broker configuration"). Plus: `export ORACLE_SID=PRIM1` przesunięte na początek skryptu (przed sqlplus w 0b) — greenfield `.bash_profile` ma to po S28-41, ale defensywnie wymuszamy też dla nieinterakcyjnych runów. |
| **S28-45** | ✅ DONE | `docs/07_FSFO_Observers_PL.md`, `scripts/setup_observer.sh` | **3 bugi w docs/07 + setup_observer.sh przed odpaleniem etapu Observer**: (1) `cp /tmp/response_files/client.rsp /tmp/response_files/client.rsp` (linia 16) — same source/target, no-op. Naprawione na `ls /tmp/scripts/setup_observer.sh /tmp/response_files/client.rsp` z komentarzem konwencji repo→/tmp przez MobaXterm. (2) `SERVICE_NAME = PRIM_DGMGRL` (bez `.lab.local`) w aliasach `PRIM_ADMIN`/`STBY_ADMIN` — Oracle 23.26.1 z `db_domain=lab.local` rejestruje serwisy jako `NAZWA.lab.local` (znane FIX-040/S28-29). Bez sufiksu → `ORA-12514` przy `dgmgrl /@PRIM_ADMIN`. Naprawione w docs/07 linie 113/118 i setup_observer.sh linie 143/148. (3) Brak `LOAD_BALANCE=off + FAILOVER=on` w PRIM_ADMIN ADDRESS_LIST — lekcja S28-38, deterministyczny connect do RAC zamiast random load-balance. Dodane do docs/07 i setup_observer.sh. (4) Konsolidacja: docs/07 linia 19 z `bash setup_observer_infra01.sh` na `bash setup_observer.sh` (defaults = master), z notką że `_infra01.sh` zostaje jako thin wrapper (backward compat). |
| **S28-46** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **STBY niezarejestrowany w HAS (Oracle Restart) na stby01** — RMAN DUPLICATE nie wpisuje bazy do HAS. Po RMAN: `srvctl status database -db STBY` → `PRCD-1120: resource for database STBY could not be found`, `crsctl stat res -t` na stby01 pokazuje tylko `ora.evmd` + `ora.ons` (puste minimum HAS, brak `ora.stby.db`, `ora.LISTENER` itp.). Konsekwencje: (a) `srvctl modify database -db STBY -startoption 'READ ONLY'` w kroku 7 rzuca PRCD-1120 — startoption nie zostaje ustawiony; (b) po reboot stby01 baza NIE wstaje automatycznie, trzeba ręcznie sqlplus + STARTUP. Fix: nowy krok 7b (między SAVE STATE a srvctl modify) wykonuje `srvctl add database -db STBY -oraclehome ... -spfile $OH/dbs/spfileSTBY.ora -role PHYSICAL_STANDBY -startoption MOUNT -policy AUTOMATIC -domain lab.local`. Idempotentny — `\|\| true` na re-run gdy już zarejestrowana. Po add → srvctl modify działa, baza ma auto-start MOUNT po reboot, broker przejmuje i ustawia READ ONLY WITH APPLY. |
| **S28-47** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **HAS pokazuje `ora.stby.db OFFLINE OFFLINE` mimo działającej bazy** — `srvctl add database` (S28-46) rejestruje tylko metadata, nie startuje. Faktyczny proces bazy chodzi spoza HAS (uruchomiony wcześniej przez RMAN+ALTER DATABASE OPEN). HAS nie potrafi wykryć "tej już chodzącej" — musi być explicit handoff. Bez tego po pierwszym reboot stby01 baza dalej nie wstaje (HAS widzi resource OFFLINE i traktuje jako "stop wymagany"). Fix: nowy krok 7c (po `srvctl modify`) wykonuje sekwencję handoff: `EDIT DATABASE 'stby' SET STATE='APPLY-OFF'` (broker stop apply) → `SHUTDOWN IMMEDIATE` (sqlplus na stby01) → `srvctl start database -db STBY` (HAS startuje w READ ONLY z startoption ustawionym w S28-46) → sleep 10 → `EDIT DATABASE 'stby' SET STATE='APPLY-ON'`. Final sanity check: `crsctl stat res ora.stby.db -t` musi pokazać `ONLINE ONLINE stby01 Open Read Only,STABLE`. |
| **S28-48** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md`, `OPERATIONS.md` | **PRCR-1055 "Cluster membership check failed for node stby01" przy `srvctl start database -db STBY`** — CSSD na stby01 było OFFLINE. Diagnoza: `crsctl check css` → `CRS-4530: Communications failure contacting CSS daemon`. Root cause: install GI ze `CRS_SWONLY` + `roothas.pl` na stby01 zarejestrował resource `ora.cssd` ale NIE auto-startuje CSSD po boot VM. `crsctl stat res -t` pokazywał: `ora.cssd OFFLINE OFFLINE`, `ora.evmd ONLINE`, `ora.ons OFFLINE`, `ora.diskmon OFFLINE`. Fix manual: `crsctl start resource ora.cssd -init` (`-init` flag wymagany dla low-level cluster resources) → po 15s CSSD ONLINE → `srvctl start database` zadziałał. Fix w skrypcie: nowy krok 7a (przed `srvctl add database` w S28-46) sprawdza `crsctl status resource ora.cssd -init`; jeśli OFFLINE → `crsctl start resource ora.cssd -init` + sleep 15 + verify. Dodano też do OPERATIONS.md w cold START krok 3 (stby01) jako KRYTYCZNE przed weryfikacją HAS. UWAGA: wynika z tego że po każdym boot stby01 trzeba ręcznie startować CSSD (lub dodać systemd unit `oracle-cssd.service` który robi to automatycznie — opcjonalna optymalizacja na przyszłość). |
| **S28-68** | ✅ DONE | `AUTONOMOUS_ACCESS_LOG.md` (Final Validation + Executive Summary) | **Scenariusz 6 (final validation): wszystkie 5 warstw PASS** po wszystkich testach. OS: 16 PASS 0 FAIL. Cluster: RAC complete (3 SCAN listeners), Oracle Restart complete (cssd + listenery). Broker: SUCCESS, MaxAvailability+SYNC, FSFO Zero Data Loss z 3 obserwerami. TAC: MYAPP_TAC running PRIM1+PRIM2 z atrybutami LEVEL1. test_log: 50466 rows łącznie (366 z scen.1, 50000 z scen.4 lag, 100 z scen.3 TAC replay z **0 duplikatów**). Sesja Krok 3: 6 scenariuszy w ~90min autonomous (vs ~6-8h manual). 6 actual failoverów/switchoverów + auto-reinstate flawlessly. Wszystkie bugi (S28-64..S28-67) naprawione w toku, wpisane do EXECUTION_LOG_PL + AUTONOMOUS_ACCESS_LOG. Środowisko 100% operacyjne, 0 data loss. |
| **S28-67** | ✅ DONE | `AUTONOMOUS_ACCESS_LOG.md` (Scenariusz 4) | **Lekcja edukacyjna: `FastStartFailoverLagLimit` chroni Transport Lag, NIE Apply Lag.** Test: `EDIT DATABASE stby SET STATE=APPLY-OFF` + 50000 INSERT na PRIM → Apply Lag urósł do 5 min 10s, ale Transport Lag pozostał 0. Broker SUCCESS, `FAILOVER TO stby` zadziałał (nie odmówił). Redo dotarł do stby (Transport=0), tylko nieaplikowany — broker uznaje że dane bezpieczne, dokończy apply po promote. Aby naprawdę zablokować failover w teście: `EDIT DATABASE PRIM SET STATE=TRANSPORT-OFF` — wtedy redo nie doszedł, Transport Lag rośnie. **W produkcji prawdziwa ochrona przed data loss = Maximum Protection mode** (Maximum Availability nie wystarczy mimo LagLimit=0). Bonus: Auto-Reinstate=TRUE zadziałał — po failover, manual `STARTUP MOUNT` na PRIM → broker sam reinstatuje jako standby; `SWITCHOVER TO PRIM` przywrócił RAC=Primary. |
| **S28-66** | ✅ DONE | `AUTONOMOUS_ACCESS_LOG.md` (Scenariusz 5) | **DGMGRL syntax change w 26ai dla MASTEROBSERVER promote.** Próba `EDIT CONFIGURATION SET MASTEROBSERVER TO obs_dc` (składnia 19c) → `Syntax error before or at "MASTEROBSERVER"`. Poprawna składnia 26ai (z `HELP SET`): `SET MASTEROBSERVER TO <observer-name>;` jako top-level command (nie wewnątrz EDIT CONFIGURATION). Plus obserwacja: systemd `Restart=on-failure RestartSec=10s` na `dgmgrl-observer-*.service` jest **tak efektywny** że uniemożliwia demonstrację awarii Master Observera przez `pkill -9` — proces wraca w 10s, broker miga pingiem ale nie traci kontaktu długo by promować. Workaround: `systemctl stop` (wymaga root@infra01) lub explicit `SET MASTEROBSERVER TO <backup>` (działa w każdej chwili, dowodzi Multi-Observer redundancji). |
| **S28-65** | ✅ DONE | `docs/09_Test_Scenarios_PL.md` (Scenariusz 3 walidacja), `AUTONOMOUS_ACCESS_LOG.md` | **2 bugi routing services po wielokrotnych switchover** ujawnione przy próbie testu Scenariusza 3 (TAC replay). (1) **Service `-role PRIMARY` na Oracle Restart (SI) nie auto-stopuje** po role swap — gdy stby (SI) zostaje physical standby, `MYAPP_TAC` pozostaje running, JDBC FAILOVER=on łączy tam i ORA-16000 (RO database). Fix: `srvctl stop service` na stby. (2) **SCAN listener `BLOCKED service`** po role swap — service zarejestrowany ale BLOCKED, klient ORA-12514. Fix: `srvctl stop/start service -db PRIM` clean re-rejestracja. **Test wynik:** 100 INSERT × 1s, kill -9 SPID na PRIM2 podczas iter 26 → 3-sekundowy gap (replay) → kontynuacja → BATCH COMMITTED 100 rows + 0 duplikatów + replayCount=0 widzialne w aplikacji. Application Continuity Level 1 zadziałał transparentnie. |
| **S28-64** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **Auto-OPEN APPPDB w READ WRITE po role swap — `srvctl modify pdb -policy AUTOMATIC -role PRIMARY`.** Po manualnym switchover (Scenariusz 1) odkryto że APPPDB pozostaje w READ ONLY na nowym primary mimo że SAVE STATE było uruchomione na obu DB. Powód: SAVE STATE zachowuje stan **per-rola w danym momencie** — gdy stby był standby (READ ONLY przez Active DG), state został zapisany jako READ ONLY, i po swap PRIM jako primary też próbował open w READ ONLY → ORA-65054 "CDB read only mode". Klasyczne workaroundy: ręczny CLOSE/OPEN READ WRITE + SAVE STATE po każdym switchover (clunky), albo system trigger AFTER STARTUP. **Modernizacja w 26ai:** `srvctl modify pdb` traktuje PDB jako CRS resource — z `policy=AUTOMATIC + role=PRIMARY` CRS otwiera PDB w READ WRITE TYLKO gdy DB jest Primary; gdy DB w roli Standby, CRS nic nie robi (Active DG zarządza READ ONLY). Self-healing: dowolna zmiana roli → CRS reaguje przy następnym start instancji. Konfiguracja jednorazowa per DB (PRIM RAC + STBY Oracle Restart). |
| **S28-63** | ✅ DONE | `docs/08_TAC_and_Tests_PL.md` (Krok 5.1) | **`ORA-17868: Unknown host specified.: MYAPP_TAC: Name or service not known`** przy `java TestHarness`. JDBC thin driver NIE czyta env vars (w tym `TNS_ADMIN`) — czyta tylko system properties JVM. Mimo `export TNS_ADMIN=/opt/lab/tns` w `~/.bash_profile`, JDBC szukał `MYAPP_TAC` jako hostname DNS. Fix: `-Doracle.net.tns_admin=/opt/lab/tns` w komendzie `java` (przed `-cp`). Po fix: TestHarness łączy się przez UCP, baseline test 30s pokazuje SUCCESS na PRIM1+PRIM2 (UCP per-connection load balance). Lekcja udokumentowana w docs/08 Krok 5.1 z ostrzeżeniem. |
| **S28-62** | ✅ DONE | `scripts/setup_cross_site_ons.sh`, `docs/08_TAC_and_Tests_PL.md` (Krok 3, 3.a) | **3 luki przy faktycznym deployu cross-site ONS:** (1) `srvctl: command not found` gdy uruchomione jako root — root nie ma grid env; fix: `su - grid -c "srvctl modify ons ..."`. (2) `ssh oracle@stby01 'sudo bash -s'` zakładał NOPASSWD sudo dla oracle (brak); fix: zmienione na `ssh root@stby01 'bash -s'` (wymaga root SSH key na stby01). (3) `oracle-ons.service` `status=203/EXEC` — onsctl wymaga LD_LIBRARY_PATH+PATH, nie tylko ORACLE_HOME (Environment= w unicie nie wystarcza); fix: wrapper script `/usr/local/bin/{start,stop}-ons.sh` analogicznie do S28-54 dla observera. (4) `loglevel` i `useocr` w ons.config są UNKNOWN keys w 26ai (warning `unkown key:` w onsctl ping); fix: usunięte z ons.config. Wszystkie 4 fixy w skrypcie i w docs/08 Krok 3 + 3.a (z 3.a podziałem na 3.a.1 wrapper + 3.a.2 unit). |
| **S28-61-bis** | ✅ DONE | `docs/08_TAC_and_Tests_PL.md` (Metoda 1) | **Dodano noticebox "Co skrypty robią pod spodem"** w Metodzie 1 — operator wybierający ścieżkę auto wie że (a) `setup_tac_services.sh` jest idempotentny przez F-12, post-create verify, auto-rejestracja stby01; (b) `setup_cross_site_ons.sh` ma zaszyte FIX-040 (-clusterid removed), FIX-082 Luka 2 (ons.config nodes), FIX-083 (systemd persistence). Cel: transparentność dla użytkownika auto-mode bez konieczności czytania kodu skryptu. |
| **S28-61** | ✅ DONE | `docs/08_TAC_and_Tests_PL.md` (Krok 1, 1.5, 2, 3, 5) | **Audyt docs/08 vs scripts + VMs/FIXES_LOG ujawnił 5 luk:** (1) Krok 1/1.5 brak noticebox o idempotency `PRCD-1126` (skrypt F-12 obsługuje, manual nie); (2) Krok 2 brak ostrzeżenia o usuniętej fladze `-clusterid` w 26ai (VMs/FIX-040) i braku firewall portu 6200 (VMs/FIX-011); (3) Sekcja 3 brak pre-flight `nc -zv stby01 6200` + `onsctl ping` (VMs/FIX-080 F5/F7); (4) Sekcja 3 brak uzasadnienia użycia wariantu `_26ai` SQL (`GV$REPLAY_STAT_SUMMARY` removed — VMs/FIX-082 Luka 1); (5) Sekcja 5 brak KRYTYCZNEGO ostrzeżenia o `oracle.jdbc.replay.OracleDataSourceImpl` vs `pool.OracleDataSource` (VMs/FIX-084 F1) — bez tego klient dostanie ORA-03113 zamiast TAC replay. Wszystkie 5 fixów dodane jako noticeboxy w docs/08. TestHarness.java już ma poprawne `setConnectionFactoryClassName` + `setValidateConnectionOnBorrow(true)` (potwierdzone). |
| **S28-60** | ✅ DONE | `docs/07_FSFO_Observers_PL.md` (Sekcja 6.2) | **Sekcja 6.2 (obs_dr na stby01) miała tylko skrót "Powtórz 6.1 z podmianami"** — niezgodne z duchem ścieżki manualnej (operator chce widzieć każdy krok bez konieczności mentalnego mapowania). Fix: pełne kroki 6.2.a-e analogicznie do 6.1 — kopiowane bloki kodu z podmienionymi nazwami `obs_dr`/`stby01`. Wzrost objętości docs/07 ~120 linii, ale zgodnie z preferencją użytkownika dla czytelności manualnej. |
| **S28-59** | ✅ DONE | `scripts/setup_observer.sh`, `docs/07_FSFO_Observers_PL.md` (Sekcja 6) | **`ADD OBSERVER obs_dc ON HOST 'prim01.lab.local'` rzuca `Syntax error before or at "OBSERVER"` w 26ai DGMGRL.** Polecenie `ADD OBSERVER ... ON HOST '...'` było składnią 19c i wcześniejszą — w 26ai broker auto-rejestruje observera przy `START OBSERVER` (wywoływane przez wrapper systemd przy `systemctl start`). Master już Active → broker przypisze nowego jako Backup automatycznie. Fix: usunięto blok dgmgrl `ADD OBSERVER` z `else` branch (`OBSERVER_ROLE=backup`), zostaje tylko `systemctl enable --now ${SVC_UNIT}` + `sleep 10`. Plus aktualizacja docs/07 sekcja 6 noticebox z lekcją. Powiązane: VMs/FIX-074/075. |
| **S28-58** | ✅ DONE | `docs/07_FSFO_Observers_PL.md` (Metoda 1 + Sekcja 6) | **Restrukturyzacja docs/07 — backup observerzy w obu metodach.** Stan poprzedni: Metoda 1 (auto) opisywała tylko Master infra01; Sekcja 6 (backup) i tak używała wywołań skryptu — niespójne ze ścieżką "manualną krok-po-kroku". Fix: (1) Metoda 1 rozbudowana do 4 podpunktów: 1.1 master infra01, 1.2 obs_dc prim01, 1.3 obs_dr stby01, 1.4 weryfikacja `SHOW OBSERVERS` (3 rekordy); (2) Sekcja 6 przepisana na pełne kroki manualne 6.1.a-e (mkdir, tnsnames+sqlnet, wallet 4 cred, wrappery, unit systemd) dla `obs_dc` na prim01; 6.2 daje regułę powtórzenia dla `obs_dr` z podmianą; 6.3 verification. Plik teraz spójny: auto = Metoda 1 (4 polecenia łącznie), manual = Metoda 2 + Sekcja 6 (krok-po-kroku dla wszystkich 3 obserwerów). |
| **S28-57** | ✅ DONE | `scripts/setup_observer.sh`, `docs/07_FSFO_Observers_PL.md` (Krok 4) | **`DGM-16979 Authentication failed` przy attach observera do PRIM/STBY** mimo poprawnych aliasów (S28-56). Root cause: SQLNET.WALLET_OVERRIDE=TRUE — Oracle używa wallet credentials zamiast pytać o pwd. Wallet miał tylko PRIM_ADMIN/STBY_ADMIN, brakowało `PRIM` i `STBY`. Observer próbuje `connect /@PRIM` → wallet lookup misses → auth fails. Fix: `ensure_cred PRIM` + `ensure_cred STBY` w skrypcie i docs/07. Symptom widoczny w log observera: `[P001] Failed to log into PRIM. Authentication failed.` → broker un-registered observer → `SHOW CONFIGURATION` pokazywał `ORA-16819: fast-start failover observer not started` mimo że systemd unit aktywny. |
| **S28-56** | ✅ DONE | `scripts/setup_observer.sh`, `docs/07_FSFO_Observers_PL.md` (Krok 3) | **Observer wystartował ale nie attach do PRIM/STBY** — log `start-observer-obs_ext.sh[..]: ORA-12154: Cannot find alias PRIM in /etc/oracle/tns/obs_ext/tnsnames.ora` + `Failed to attach to PRIM/STBY`. Root cause: tnsnames observera miał tylko `PRIM_ADMIN`/`STBY_ADMIN` (port 1522 → LISTENER_DGMGRL → broker control). Ale po START OBSERVER, broker zwraca observerowi `DGConnectIdentifier` (default = db_unique_name = `PRIM`/`STBY`) i observer próbuje `connect /@PRIM`/`/@STBY` przez **port 1521** (LISTENER, regular service). Brak aliasów → ORA-12154 → observer "started" ale w pełni dysfunkcyjny (nie potrafi pingować baz, nie może wykryć failover). Fix: dodano `PRIM`/`STBY` aliasy z PORT=1521 + SERVICE_NAME=PRIM.lab.local / STBY.lab.local — w skrypcie i docs/07. Po fix: observer attach OK, `SHOW OBSERVERS` zwraca master rekord. |
| **S28-55** | ✅ DONE | `scripts/setup_observer.sh`, `docs/07_FSFO_Observers_PL.md` (Krok 4) | **Idempotency wallet credentials** — `mkstore -createCredential` rzuca `OracleSecretStoreException: Credential already exists` przy re-run skryptu (z `set -euo pipefail` → exit). Fix: helper `ensure_cred(ALIAS)` używa `-listCredential | grep -q ALIAS` jako branch — istnieje → `-modifyCredential`, brak → `-createCredential`. Plus uproszczenie: pwd przez `printf '%s\n' | mkstore` zamiast nested heredocs (czytelniejsze, mniej escape'owania). Wallet `-create` chroniony przez `if [ ! -f $WL/cwallet.sso ]`. Ten sam pattern w docs/07 Krok 4 wariant nieinteraktywny. |
| **S28-54** | ✅ DONE | `scripts/setup_observer.sh`, `docs/07_FSFO_Observers_PL.md` (Krok 5) | **`status=203/EXEC` w `dgmgrl-observer-obs_ext.service` po systemctl start** mimo że `dgmgrl` jest executable i Environment ORACLE_HOME ustawione w unicie. Root cause: `ExecStart=` z embedded single-quotes `'/var/log/.../obs_ext.dat'` w double-quoted argumencie `"START OBSERVER ... FILE IS '...' LOGFILE IS '...'"` — parser systemd nie radzi sobie z tym tokenowaniem (różnica vs `bash`/`/bin/sh`). FIX-074/075 z VMs zakładał starszą wersję parsera. Fix: wrapper scripts `/usr/local/bin/{start,stop}-observer-${OBSERVER_NAME}.sh` (chmod 755) — bash-owy ENV setup + `exec dgmgrl ...` (cudzysłowy obsłużone natywnie). Unit systemd używa tylko ścieżek do wrapperów. Test: `systemctl status` → `active (running)`, `dgmgrl /@PRIM_ADMIN "SHOW OBSERVERS"` → 1 rekord. Plus dgmgrl w ExecStart musi mieć WSZYSTKIE Oracle env vars (ORACLE_HOME, LD_LIBRARY_PATH, TNS_ADMIN, NLS_LANG) — Environment= w unicie czasem nie wystarcza pod sudo/systemd ścieżką, dlatego wrapper eksportuje je explicit. |
| **S28-53** | ✅ DONE | `scripts/setup_observer.sh`, `docs/07_FSFO_Observers_PL.md` (Krok 4) | **`mkstore -create -p <pwd>` w 26ai NIE działa — pwd MUSI iść przez stdin.** Pierwsza próba: heredoc `<<'WALLETEOF'` (z apostrofami) blokował ekspansję `${LAB_PASS}` → `mkstore` dostawał `-p ""` i prosił interaktywnie (`PKI-01002`/`01003`). Druga próba (usunięcie apostrofów + outer-shell expansion `${LAB_PASS}` na argv) — to samo, bo `-p` w 26ai mkstore jest IGNORED dla `-create`/`-createCredential`. Ostateczny fix (wzorzec VMs/FIX-071): build temp script `/tmp/wallet_setup.XXX.sh` (chmod 700, owned oracle, trap rm -f), pwd idzie do mkstore przez heredoc `<<MKEOF\n$WP\n$WP\nMKEOF` (2× dla `-create`, 1× dla `-createCredential`). Plus uwagi: (a) `cwallet.sso` jest AUTO-tworzone przez `-create` w 26ai (`-autoLogin`/`-createSSO` opcjonalne); (b) docs/07 Krok 4 pokazuje DWA warianty — interaktywny (operator wpisuje pwd) i nieinteraktywny (stdin heredoc). Pułapka udokumentowana w docs/07 jako noticebox. |
| **S28-52-bis** | ✅ DONE | `kickstart/ks-infra01.cfg`, `docs/02b_OS_Preparation_Manual_PL.md` (sekcja 1.4a + 1.5) | **PRVG-1205 / INS-13014 na infra01 podczas runInstaller Oracle Client** — infra01 nie ma `oracle-database-preinstall-23ai` (to nie host DB), więc kernel sysctl mają domyślne RHEL/OL wartości (file-max=779285, rmem_default=212992, aio-max-nr=65536) zamiast Oracle-recommended. Z `-ignorePrereqFailure` install się dokończył (dgmgrl OK), ale dla porządku należy ustawić te parametry. Fix: dodano `/etc/sysctl.d/97-oracle-client.conf` (fs.file-max=6815744, fs.aio-max-nr=1048576, net.core.{r,w}mem_default=262144, rmem_max=4194304, wmem_max=1048576, ip_local_port_range) — w kickstarcie ks-infra01.cfg (greenfield) i w docs/02b sekcja 1.4a (manual). Plus naprawione ścieżki Observer w obu plikach: `tns/ext` → `tns/obs_ext`, `wallet/observer-ext` → `wallet/obs_ext`, `log/oracle/observer` → `log/oracle/obs_ext` (spójność z OBSERVER_NAME=obs_ext default w setup_observer.sh — ostatecznie domknięte z S28-51). |
| **S28-52** | ✅ DONE | `response_files/client.rsp` | **INS-10105 cvc-complex-type w `client.rsp` na infra01** — F-23 z FIXES_PLAN_v2_PL dodało `oracle.install.option=INSTALL_CLIENT` i `oracle.install.client.rootconfig.executeRootScript=false` zakładając że to klient analog do DB schema. Błąd: `rspfmt_clientinstall_response_schema_v23.0.0` w 26ai jest STRICT i nie ma tych kluczy (są tylko w `rspfmt_dbinstall_response_schema_v23.0.0`). Allowed keys (z VMs/FIXES_LOG FIX-070): `oracle.install.responseFileVersion`, `UNIX_GROUP_NAME`, `INVENTORY_LOCATION`, `ORACLE_HOME`, `ORACLE_BASE`, `oracle.install.client.installType`. Plus opcjonalne: `SELECTED_LANGUAGES`, `ORACLE_HOSTNAME`, `oracle.install.IsBuiltInAccount`, PROXY_*, `oracle.install.client.oramtsPortNumber`, `customComponents`, `schedulerAgent*`, `drdaas.*`. Fix: rollback do v2.2 — minimalny rsp z 5 kluczami + responseFileVersion. F-23 oznaczone jako REVERTED. Lekcja: schema 23.0.0 client ≠ db; każdy nadmiarowy klucz = INS-10105. |
| **S28-51** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md`, `docs/07_FSFO_Observers_PL.md` | **MaxAvailability + LogXptMode=SYNC alignment z FastStartFailoverLagLimit=0** — broker po `ENABLE CONFIGURATION` w skrypcie pozostawał w `MaxPerformance` (default), ale `setup_observer.sh` ustawia `FastStartFailoverLagLimit=0` (Zero Data Loss). Mismatch: ASYNC apply zawsze ma pewny lag → FSFO blokowany przy KAŻDEJ awarii (Scenariusz 4 z docs/09 by zadziałał, ale nie udałby się Scenariusz 2 — failover blokowany). Architektura w docs/01 + ARCHITECTURE_DIAGRAMS_PL.md + index_PL.html (`MaxAvailability mode, SYNC redo`) zakładają ten tryb. Fix: nowy krok 6e w skrypcie i docs/06 (po ENABLE CONFIGURATION): `EDIT DATABASE 'PRIM' SET PROPERTY 'LogXptMode'='SYNC'` + `EDIT DATABASE 'stby' SET PROPERTY 'LogXptMode'='SYNC'` + `EDIT CONFIGURATION SET PROTECTION MODE AS MAXAVAILABILITY`. Po fix: Scenariusze 1-6 w docs/09 działają zgodnie z opisem. Plus naprawione path inconsistencies w docs/07 (`observer-ext` → `obs_ext` + `/var/log/oracle/observer` → `/var/log/oracle/obs_ext` — spójność z `setup_observer.sh` używającym `OBSERVER_NAME=obs_ext` jako default). |
| **S28-50** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md`, `OPERATIONS.md` | **LISTENER (port 1521) na stby01 jako HAS resource — auto-start po reboot.** Po cold restart `lsnrctl status LISTENER` na stby01 → `TNS-12541: No listener` (listener nie wstał). Na PRIM nie udaje się shippować redo → `ORA-16778: Redo transport error`. Root cause: `CRS_SWONLY` install + `roothas.pl` na stby01 NIE tworzy `ora.LISTENER.lsnr` automatycznie (w przeciwieństwie do prim01/02 gdzie pełny GI install z RAC ma to natywnie). Listener startował tylko ręcznie z DB_HOME (oracle) — po reboot tracony. Fix: krok 2 skryptu zmieniono — listener.ora dla LISTENER (1521) deployowany w GRID_HOME na stby01 (grid user, append do existing), `srvctl add listener -listener LISTENER -endpoints 'TCP:1521'` jako grid (HAS resource), `srvctl start listener`. tnsnames.ora pozostaje w DB_HOME (oracle, dla sqlplus). Pułapka recovery: `srvctl add listener` rzuca `PRCN-2065: Port 1521 not available` jeśli manual listener jeszcze chodzi → najpierw `lsnrctl stop` z oracle. Po deployu: `crsctl stat res ora.LISTENER.lsnr` → ONLINE ONLINE stby01, broker SUCCESS, RFS odbiera redo. |
| **S28-49** | ✅ DONE (CRS/HAS-managed) | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md`, `OPERATIONS.md` | **`LISTENER_DGMGRL` (port 1522) — best practice CRS/HAS auto-start.** Fundamentalna luka: skrypt nie tworzył tego listenera w ogóle. Konsekwencje: (a) StaticConnectIdentifier brokera (PORT=1522) wskazywał na nieistniejący endpoint — przy switchover/failover/observerach `ORA-12514`/`TNS-12541`; (b) `dgmgrl /@PRIM_ADMIN` z infra01 nie podłącza → Multi-Observer niemożliwy. Diagnoza: `lsnrctl status LISTENER_DGMGRL` na 3 nodach → `TNS-01101: Could not find listener name`. **Fix CRS/HAS-managed (auto-start po reboot):** (1) Listener.ora w GRID_HOME (grid user) na 3 nodach z definicją `LISTENER_DGMGRL` + `SID_LIST_LISTENER_DGMGRL` mapping `<DB>_DGMGRL.lab.local → SID_NAME` (PRIM1/PRIM2/STBY); (2) `srvctl add listener -listener LISTENER_DGMGRL -endpoints 'TCP:1522'` jako grid — w RAC cluster-wide, w HAS lokalnie; (3) `srvctl start listener` — auto-restart=always domyślnie po `srvctl add`. **Pułapka:** `srvctl add listener -user oracle` rzuca PRCN-2018 (wymaga root) — dlatego listener.ora w GRID_HOME a nie DB_HOME, listener uruchamiany przez grid user (SID_LIST z `ORACLE_HOME=DB_HOME` działa cross-home). Skrypt w kroku 4a robi to dla prim01/prim02 (RAC), w 4b dla stby01 (HAS), w 4c dodatkowo `crsctl modify resource ora.cssd -attr 'AUTO_START=always' -init` (FIX-S28-48 trwale). Plus poprawa GLOBAL_DBNAME w docs/06 (`.lab.local` — S28-29 spójność). Po greenfield re-runie wszystko wstaje samo po reboot — bez ręcznego `lsnrctl start`. |
