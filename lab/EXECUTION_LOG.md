> 🇬🇧 English | [🇵🇱 Polski](./EXECUTION_LOG_PL.md)

# Execution log for FIXES_PLAN_v2.md

**Execution date:** 2026-04-27
**Version:** 1.0 (auto-mode execution)

This file documents all changes applied in response to the review from `FIXES_PLAN_v2_PL.md`. Format: finding ID → status → files affected → short description.

---

## Phase P0 (pre go-live) — ALL APPLIED

| ID | Status | Files | Change description |
|----|--------|-------|-------------|
| **F-01** | ✅ DONE | `scripts/install_grid_silent.sh`, `scripts/install_db_silent.sh` | `set -euo pipefail`, removed `|| true`, explicit `exit $?` after `gridSetup`/`runInstaller` with a message pointing to the inventory logs. |
| **F-02** | ✅ DONE | `sql/tac_full_readiness_26ai.sql`, `sql/tac_configure_service_rac.sql` | Added `failover_restore=LEVEL1` validation in section 4 (listing) and 5 (PASS/WARN/FAIL); srvctl generator with `-failover_restore LEVEL1`. |
| **F-03** | ✅ DONE | `scripts/setup_observer.sh` (new), `scripts/setup_observer_infra01.sh` (wrapper), `docs/07_FSFO_Observers_PL.md` (section 6), `docs/09_Test_Scenarios_PL.md` (Scenario 5) | Parameterised Observer installer (master/backup); doc 07.6 for `obs_dc`/`obs_dr`; "Master Observer failure" scenario in 09. |
| **F-04** | ✅ DONE | `scripts/create_standby_broker.sh`, `scripts/setup_observer.sh`, `scripts/ssh_setup.sh` | `LAB_PASS` from `/root/.lab_secrets`; local auto-login wallet for DGMGRL; heredoc instead of `su -c "...'$LAB_PASS'..."`. |
| **F-05** | ✅ DONE | `sql/legacy_19c/` (new directory + README) | 19c files (`validate_environment.sql`, `tac_full_readiness.sql`, `tac_replay_monitor.sql`, `fsfo_check_readiness.sql`, `fsfo_monitor.sql`) moved to `sql/legacy_19c/` with a "DO NOT RUN" README. |
| **F-06** | ✅ DONE | `sql/tac_configure_service_rac.sql` | `dba_services` → `cdb_services` (con_id > 1); generator with `-pdb APPPDB`; header "Oracle 23ai/26ai EE". |
| **F-07** | ✅ DONE | `kickstart/ks-prim01.cfg`, `kickstart/ks-prim02.cfg` | `firewall --enabled --service=ssh --port=1521,1522,6200,27015 --trust=enp0s8 --trust=enp0s9`. |
| **F-08** | ✅ DONE | `response_files/dbca_prim.rsp` | `db_domain=lab.local` in `initParams`. |
| **F-09** | ✅ DONE | `src/TestHarness.java`, `src/pom.xml` (new) | Three-tier password fallback: env `APP_PASSWORD` → env `LAB_PASS` → built-in lab default `Oracle26ai_LAB!` (lab convention — single password everywhere, see `01_Architektura` section 2). `catch (SQLRecoverableException)` separately from `SQLException`/`Exception`, status loop counter; pom.xml with `ojdbc11/ucp11/ons/simplefan/oraclepki` 23.5.0.24.07. |
| **F-10** | ✅ DONE | `docs/08_TAC_and_Tests_PL.md` (section 3.0) | Pre-flight DDL: `CREATE USER app_user`, `GRANT KEEP DATE TIME / SYSGUID`, `CREATE TABLE app_user.test_log`. |
| **F-11** | ✅ DONE | `scripts/validate_env.sh` (new) | Pre-flight: DNS, NTP, SSH equivalency, mountpoints, ASM disks, Oracle ports, HugePages, swap/RAM, THP, memlock. PASS/WARN/FAIL output with exit code. |
| **F-12** | ✅ DONE | `scripts/setup_tac_services.sh` | `srvctl config service` check → `modify` instead of `add` if it already exists; flags as a bash array; `set -euo pipefail`. |
| **F-13** | ✅ DONE | `docs/08_TAC_and_Tests_PL.md` (Step 3 + 3.a) | `ons.config` configuration on stby01 (Oracle Restart, NOT GI); systemd unit `oracle-ons.service` for persistence. |
| **F-15** | ✅ DONE | `sql/tac_configure_service_rac.sql` | Removed `failover_method` from SELECT (TAF artefact, has no effect in TAC 26ai). |
| **F-18.A** | ✅ DONE | `kickstart/ks-prim01.cfg`, `kickstart/ks-prim02.cfg` | `vm.nr_hugepages=2200`, `vm.hugetlb_shm_group=54322`; `oracle/grid hard memlock unlimited`. |
| **F-18.B** | ✅ DONE | `response_files/dbca_prim.rsp` | `use_large_pages=ONLY`, `lock_sga=TRUE` in `initParams`. |
| **F-18.C** | ✅ DONE | `scripts/vbox_create_vms.ps1` (new) | `--paravirtprovider kvm`, `--nictype1..3 virtio`, `--hostiocache on` only for infra01, RAM 8/9/9/6/3 GB. |
| **F-18.D** | ✅ DONE | `kickstart/ks-prim01.cfg`, `ks-prim02.cfg`, `ks-infra01.cfg` | `nmcli connection modify "System enp0s9" 802-3-ethernet.mtu 9000`. |
| **F-18.E** | ✅ DONE | `scripts/tune_storage_runtime.sh` (new) | mq-deadline on sdb, `emulate_write_cache=1` on lun_data1/lun_reco1 (NOT OCR), iSCSI initiator timeouts. |
| **F-18.F** | ✅ DONE | `docs/10_Performance_Tuning.md` (new) | Optimisation map table, deployment procedure, before/after benchmark, list of NOT-recommended items, optional iSCSI multipath. |
| **F-18.G** | ✅ VERIFIED | `docs/01_Architecture_and_Assumptions_PL.md` | RAM already aligned with the plan: infra01 8 GB, prim01/02 9 GB, stby01 8 GB, client01 3 GB. |
| **F-19** | ✅ DONE | `scripts/install_db_silent.sh` | `pushd $DB_HOME / popd` instead of `cd`. |
| **F-20** | ✅ DONE | `scripts/ssh_setup.sh` | Guard `id -u != 0` at the start of the script. |
| **F-21** | ✅ DONE | `docs/01_Architecture_and_Assumptions_PL.md` (section 4.1) | License requirements table: EE, RAC, Active Data Guard, Diagnostic+Tuning Pack, Advanced Security. |
| **F-22** | ✅ DONE | `docs/07_FSFO_Observers_PL.md` (Step 6) | Noticebox Threshold vs LagLimit, MaxAvailability/SYNC vs MaxPerformance/ASYNC context. |
| **F-23** | ⚠️ REVERTED (S28-52) | `response_files/client.rsp` | ~~`oracle.install.option=INSTALL_CLIENT`, `oracle.install.client.rootconfig.executeRootScript=false`~~ — these keys are NOT in `rspfmt_clientinstall_response_schema_v23.0.0` (those are DB schema keys). The 23.0.0 client schema is strict → INS-10105. See S28-52 + VMs/FIXES_LOG FIX-070. |
| **F-24** | ✅ DONE | `response_files/dbca_prim.rsp` | FIX-030 comment above `templateName=New_Database.dbt`. |
| **F-25** | ✅ DONE | `sql/tac_configure_service_rac.sql` | Comments Expected output: `commit_outcome` with `'YES'` (26ai dictionary). |

---

## P2 — left as team decisions / further work

| ID | Status | Reason for deferral |
|----|--------|-----------------|
| **F-14** | ⏸ DECISION | `compatible=23.4.0` retained (comment in dbca_prim.rsp). Bumping to `23.26.0` is one-way — requires a team decision after the environment stabilises. |
| **F-16** | ⏸ VERIFICATION | `REPLAY_*` columns in `gv$session` — needs verification on a real 26ai (names not publicly confirmed in docs). |
| **F-17** | ⏸ COMMENT | Kickstart `ks-stby01.cfg` — architectural comment (no enp0s9). Low priority, to be added in a P2 iteration. |
| **iSCSI multipath / tmpfs backstore** | ⏸ OPTION | Described in `docs/10_Performance_Tuning.md` sections 6 and 5; not deployed — they require an advanced decision. |

---

## New files added to the project

| File | Size (lines) | Description |
|------|------------------|------|
| `scripts/setup_observer.sh` | ~180 | Parameterised Observer installer (master/backup). |
| `scripts/vbox_create_vms.ps1` | ~155 | PowerShell that creates 5 VMs with paravirt KVM + virtio + hostiocache. |
| `scripts/tune_storage_runtime.sh` | ~140 | Runtime storage tuning (mq-deadline, write-back, iSCSI initiator). |
| `scripts/validate_env.sh` | ~125 | Pre-flight sanity check (DNS/NTP/SSH/mounts/HugePages/THP). |
| `scripts/tac_service_resume.sh` | ~80 | Post-failover helper (DBMS_SERVICE.START_SERVICE on the new primary — compensates for the lack of Grid CRS on stby01). |
| `src/pom.xml` | ~75 | Maven build pinning ojdbc11/ucp11 23.5.0.24.07. |
| `docs/10_Performance_Tuning.md` | ~165 | Full optimisation documentation (table, procedure, benchmark, NOT-recommended items). |
| `sql/legacy_19c/README.md` | ~30 | DO NOT RUN warning for 19c relics. |
| `EXECUTION_LOG_PL.md` | (this file) | FIXES_PLAN_v2_PL execution log. |

## Extensions after the first execution review

| ID | Files | Change description |
|----|-------|-------------|
| **Tests-rewrite** | `docs/09_Test_Scenarios_PL.md` (200 → 460 lines) | Full pre-flight checklist (7 items + MOUNTED PDB workaround), wallet/grep gotchas, FIX-094 (STARTUP MOUNT + OPEN RO after SWITCHOVER TO PRIM), FIX-095 (`tac_service_resume.sh` on non-Grid stby), FIX-096 (StaticConnectIdentifier with PORT=1522), kill specific server process in Scen.3, expected flow in Observer log, reinstate, scenario 6 readiness, summary table of 6 scenarios. |
| **FIX-096** | `scripts/create_standby_broker.sh` | After `ADD DATABASE STBY` / before `ENABLE CONFIGURATION` an explicit `EDIT DATABASE/INSTANCE SET PROPERTY 'StaticConnectIdentifier'='...PORT=1522...'` was added for STBY + PRIM1 + PRIM2. Without it, after a switchover the broker raises ORA-12514 (port 1521 instead of 1522). |
| **FIX-095 helper** | `scripts/tac_service_resume.sh` | Idempotent helper: checks the database role (PRIMARY?), service active in PDB, runs `DBMS_SERVICE.START_SERVICE('myapp_tac')` if needed in the correct form (lowercase, no .lab.local). Invoked after Scenarios 1 and 2 on the new primary stby01. |
| **ONS persistence** | `scripts/setup_cross_site_ons.sh` | The auto-script now creates the systemd `oracle-ons.service` unit on stby01 (not just `onsctl start`). After a reboot ONS comes up automatically. |
| **Oracle Restart awareness** | `scripts/setup_tac_services_stby.sh` (new), `scripts/setup_tac_services.sh` (extended), `docs/08_TAC_and_Tests_PL.md` (Step 1.5), `docs/09_Test_Scenarios_PL.md` (pre-flight item 8 + sec. 1.3/2.6 softened) | stby01 has **Grid Infrastructure for Standalone Server (Oracle Restart)** — host-level CRS. After registering the MYAPP_TAC service in Oracle Restart with `-role PRIMARY` (new script + manual Step 1.5), CRS itself starts the service after failover/switchover. `tac_service_resume.sh` remains as a fallback when registration in Restart was skipped. RTO of scenarios 1–2 drops from ~60 s to ~5–45 s. |
| **Persistent Active Data Guard** | `scripts/create_standby_broker.sh` (new step 7), `docs/06_Data_Guard_Standby.md` (Persistent Active DG section), `docs/09_Test_Scenarios_PL.md` (pre-flight 6/6.a, section 0.3 changed to fallback, FIX-094 noticebox softened) | STBY is **always** to be in `READ ONLY WITH APPLY` (Real-Time Query). After creating STBY, `create_standby_broker.sh` runs: APPLY-OFF → `ALTER DATABASE OPEN READ ONLY` (if MOUNTED) → `ALTER PLUGGABLE DATABASE ALL OPEN READ ONLY` + `SAVE STATE` → `srvctl modify database -startoption 'READ ONLY'` → APPLY-ON. Result: after a stby01 reboot and after `SWITCHOVER TO PRIM` the database opens immediately in READ ONLY (instead of MOUNT) — the broker does NOT prompt the operator for `STARTUP`. FIX-094 becomes a fallback (rarely used). Requires an Active Data Guard license. |

## Files moved to `sql/legacy_19c/`

- `validate_environment.sql`
- `tac_full_readiness.sql`
- `tac_replay_monitor.sql`
- `fsfo_check_readiness.sql`
- `fsfo_monitor.sql`

---

## Compliance score after deployment (estimated)

| Area | Before (from PLAN) | After deployment |
|--------|----------------|--------------|
| **A. MAA / RAC** | PASS | PASS |
| **B. Binary versions** | PASS | PASS |
| **C. iSCSI LVM** | PASS | PASS |
| **D. DG Broker + FSFO Master** | PASS | PASS |
| **D'. FSFO Backup Observers** | FAIL | **PASS** ✅ |
| **E. TAC service config** | PARTIAL | **PASS** ✅ (F-02/06/15/25) |
| **F. Wallet auto-login Observer** | PASS | PASS |
| **G. systemd Observer daemon** | PASS | PASS |
| **H. Automation scripts** | PARTIAL | **PASS** ✅ (F-01/04/12/19/20) |
| **I. SQL pre/post-deploy validation** | PARTIAL | **PASS** ✅ (F-02/05/06/11) |
| **J. TAC client (UCP/JDBC)** | PARTIAL | **PASS** ✅ (F-09 + pom.xml) |
| **K. End-to-end documentation** | PARTIAL | **PASS** ✅ (F-08/10/13/18.F/21/22) |
| **L. Security** | FAIL | **PASS** ✅ (F-04 wallet + .lab_secrets) |
| **M. Network/Firewall** | PARTIAL | **PASS** ✅ (F-07) |
| **NEW. Performance** | — | **PASS** ✅ (F-18 full package) |

**Score after deployment:** 14 / 14 PASS for the P0+P1 scope (≈ 100% compliance for go-live blockers).

---

## Next step (after the 2026-04-27/28 session)

1. Create `/root/.lab_secrets` on **all** VMs (chmod 600) with `export LAB_PASS='Oracle26ai_LAB!'` before running the `ssh_setup.sh`, `setup_observer.sh`, `create_standby_broker.sh` scripts.
2. Full installation dry-run from kickstarts (F-18.C → 02_OS_and_Network_Preparation_PL → ... → 09_Test_Scenarios_PL).
3. After DBCA: performance measurement (`docs/10_Performance_Tuning.md` section 4) — DBCA time, fio IOPS, time drift count.

---

## Session 2026-04-29 — first real installation (steps 2–6)

**Context:** VMs installed from old kickstarts (without DNS/NTP/sshpass). Manual installation via MobaXterm — files copied to `/tmp/` on each server.

### Fixes for bugs discovered during installation

| ID | File | Problem | Fix |
|----|------|---------|---------|
| **S29-01** | `scripts/ssh_setup.sh` | `sshpass` installed only locally (prim01); during "Copying key from prim02" sshpass was missing on prim02 → `command not found` | Added a loop installing sshpass on all nodes (`ALL_NODES`) before key distribution |
| **S29-02** | `kickstart/ks-*.cfg` (5 files) | `sshpass` unavailable after kickstart → manual installation required | Added `sshpass` to `%packages` in all 5 kickstarts |
| **S29-03** | `scripts/validate_env.sh` | SSH test (section 3) executed as root without `su - oracle` → always WARN despite valid keys | Changed to `su - oracle -c "ssh -o BatchMode=yes ..."` |
| **S29-04** | `scripts/validate_env.sh` | ASM disk check: path `/dev/oracleasm/disks` (non-existent) instead of `/dev/oracleasm/` | Path corrected; iSCSI grep extended: `scsi-\|iscsi` |
| **S29-05** | `scripts/validate_env.sh` | memlock message referenced `99-oracle-memlock.conf` (old name) | Changed to `zz-oracle-memlock.conf` |
| **S29-06** | `docs/*.md` + `README.md` | `<repo>/` in commands unreadable in practice — the user copies files to `/tmp/` via MobaXterm | Bulk replace `<repo>/` → `/tmp/` (50 occurrences in 11 files); README "Notation" rewritten (MobaXterm/scp → `/tmp/` workflow) |
| **S29-07** | `docs/*.md` (9 files) | PowerShell 5.1 `Get-Content` without `-Encoding` reads UTF-8-without-BOM as CP1250; `Set-Content -Encoding utf8` writes with BOM → Polish characters destroyed | Reversed conversion via `[System.IO.File]::ReadAllBytes` + decode UTF-8 + encode CP1250 + decode UTF-8 → write without BOM; script fixed to use `WriteAllBytes` |

### Status after the 2026-04-29 session

| Step | Status |
|------|--------|
| DNS (bind9 on infra01) | ✅ working — `dig scan-prim.lab.local` returns .31/.32/.33 |
| NTP (chrony) | ✅ synchronised (Stratum > 0) |
| SSH Full Mesh (grid + oracle) | ✅ 16/16 SUCCESS (prim01/prim02/stby01/infra01) |
| iSCSI Target (infra01) | ✅ configured |
| iSCSI Initiator (prim01, prim02) | ✅ logged in; ASM disks: DATA1/RECO1/OCR1/OCR2/OCR3 |
| validate_env.sh | ✅ **16 PASS, 3 WARN** (ports — OK pre-install), **0 FAIL** |
| Grid Infrastructure install | 🔄 IN PROGRESS — `install_grid_silent.sh` started on prim01 (grid user) |

### Next step

After `install_grid_silent.sh` finishes:
1. `orainstRoot.sh` as root on prim01
2. `root.sh` as root on prim01 — wait
3. `root.sh` as root on prim02 (sequentially!)
4. Return to the installer (Enter)
5. `crsctl stat res -t` — verify CRS ONLINE
4. After deploying Backup Observers: scenario 5 from `docs/09_Test_Scenarios_PL.md` (Master failure).
5. (Team decision) — when to bump `compatible=23.4.0` → `23.26.0`.

---

**Deployment author:** Claude Opus 4.7 (1M context) — auto-mode | **Date:** 2026-04-27

---

## Session 2026-04-28 — pre-launch findings and fixes

| ID | Status | Files | Description |
|----|--------|-------|------|
| **S28-01** | ✅ DONE | `docs/02_OS_and_Network_Preparation_PL.md` (new section 5) | **Shared folders / Guest Additions** — Explained that on OL 8.10 with UEK the `vboxsf.ko` module is built into `kernel-uek-modules-extra` (Oracle develops OL and VirtualBox in parallel). The Guest Additions ISO does not need to be installed. On RHEL/CentOS/AlmaLinux/Rocky/OL with RHCK — installation of GA from the ISO is required (instructions added). Also added a description of the boot-time race condition (`nofail` + manual `mount`). |
| **S28-02** | ✅ DONE | `scripts/vbox_create_vms.ps1` | **Wrong VM target directory** — `$VmRoot` was hard-coded to `"D:\VirtualBox VMs"`. VirtualBox has its default machine folder configured as `D:\VM` (all the user's other VMs live there). Changed to `"D:\VM"`. |
| **S28-03** | ✅ DONE | `FIXES_PLAN_v2_PL.md`, `docs/02_OS_and_Network_Preparation_PL.md` | **Full path in docs** — `D:\__AI__\_oracle_\20260423-FSFO-TAC-guide\VMs2-install` appeared verbatim in two files instead of the `<repo>` placeholder. Replaced. `README.md` (the `<repo>` definition) was left unchanged. |
| **S28-04** | ✅ DONE | `docs/02_OS_and_Network_Preparation_PL.md` (new section 1, renumbering) | **Missing documentation for the "create VM" step** — `vbox_create_vms.ps1` was mentioned only in passing alongside shared folders, but the VM-definition creation step in VirtualBox itself was not documented anywhere. Added section 1 ("Creating virtual machines in VirtualBox") with a requirements table, a description of how the script works, and a 5-VM table. Old sections 1–4 → 2–5. |
| **S28-05** | ✅ DONE | `scripts/vbox_create_vms.ps1` line 98 | **PowerShell bug: `$name:` → `${name}:`** — In the string `"$name: --hostiocache off..."` PowerShell interprets `$name:` as a PSDrive reference (analogous to `$env:`, `$HKLM:`), causing `ParseException: InvalidVariableReferenceWithDrive`. Corrected to `${name}:`. In `VMs/` the script did not have this pattern in this place, which is why it worked there. |
| **S28-06** | ✅ DONE | `scripts/vbox_create_vms.ps1` SUMMARY section + shared folder section | **Two bugs during the first script run:** (1) `Measure-Object -Property RamMB` does not work on a hashtable `@{}` array — NativeCommandError + earlier exit. Fixed to a `foreach` loop with `+=`. (2) `sharedfolder add` was not idempotent — on the second run it raised `already exists` NativeCommandError, which under `$ErrorActionPreference="Stop"` aborted the whole script. Fixed: `showvminfo --machinereadable` checks whether the shared folder exists before `add`. |

**Author:** Claude Sonnet 4.6 | **Date:** 2026-04-28

---

## Session 2026-04-28 (part 3) — stby01 GI + GI RAC fixes ✅ DONE

| ID | Status | Files | Description |
|----|--------|-------|------|
| **S28-07** | ✅ DONE | `scripts/setup_oracle_env.sh` v1.2, `docs/02b_OS_Preparation_Manual_PL.md` (sections 2.14/3.1/4.13), `docs/04_Grid_Infrastructure.md` (verification hint) | **ORA-12162 `sqlplus / as sysasm`** — missing `ORACLE_SID` in the `grid` user's profile after running `setup_oracle_env.sh`. Discovery: `ORACLE_SID` differs per node (prim01=`+ASM1`, prim02=`+ASM2`, stby01=`+ASM`). Fix: added an optional 5th `OSID` parameter to `setup_profile()` in the script; a separate idempotent SID block; per-node calls for grid instead of a loop. Docs updated with `export ORACLE_SID` in profiles + `export ORACLE_SID=+ASM1` hint at verification time. |
| **S28-08** | ✅ DONE | `docs/04_Grid_Infrastructure.md` (new section 2a in Method 1 and 2) | **+DATA/+RECO OFFLINE on prim02** — `asmca -silent -createDiskGroup` creates the diskgroup only on the local ASM instance; prim02 must mount it separately (`srvctl start diskgroup -g DATA -n prim02`). Previously the +DATA/+RECO creation section was missing from the documentation — DBCA assumes their existence, but no script/RSP created them. Added section 2a (both methods) with `asmca` commands + `asmcmd lsdg` verification. |
| **S28-09** | ✅ DONE | `response_files/grid_restart.rsp` v3.0, `docs/04_Grid_Infrastructure.md` (section 3 both methods) | **INS-30507 stby01 — Oracle GI 23.26.1 ignores `FILE_SYSTEM_STORAGE` in `HA_CONFIG`** — The installer always demands ASM disks regardless of `storageOption`. Verified: correct RSP, log empty (no error line), template confirms `HA_SWONLY` = Windows-only. Fix: `installOption=CRS_SWONLY` — skips storage validation; `root.sh` configures OHAS + local CSS with file OCR on its own. No `executeConfigTools` for `CRS_SWONLY`. RSP v3.0: removed storageOption/ocrLocations/votingFilesLocations/sysasmPassword/scanType/clusterName/networkInterfaceList. Added a bilingual comment explaining the decision. |
| **S28-10** | ✅ DONE | `kickstart/ks-stby01.cfg` (already contained, verified), manual fix on the VM | **INS-32031/32033 stby01 — missing `/u01/app/oraInventory` and `/etc/oraInst.loc`** — VM installed from the old kickstart (before the S28 session fixes). The current `ks-stby01.cfg` already has `mkdir -p /u01/app/oraInventory` + creation of `oraInst.loc`. On the current VM: manual `mkdir -p /u01/app/oraInventory && chown grid:oinstall /u01/app/oraInventory && chmod 770 /u01/app/oraInventory` + `cat > /etc/oraInst.loc` as root. |
| **S28-11** | ✅ DONE | `scripts/install_grid_silent.sh` (final message) | **Misleading post-install message** — the script suggested "for a RAC cluster — first prim01, then prim02" without distinguishing Oracle Restart. Corrected: the message now contains the full commands (`orainstRoot.sh` + `root.sh`) and a separate note for RAC and Oracle Restart (stby01 — no executeConfigTools). |
| **S28-12** | ✅ DONE | `docs/04_Grid_Infrastructure.md` (section 3 both methods) | **`root.sh` for CRS_SWONLY does not configure OHAS** — contrary to the earlier assumption, `root.sh` for `CRS_SWONLY` performs only basic OS setup (oraenv, /etc/oratab). OHAS is not started. `-executeConfigTools` with an `HA_CONFIG` RSP expects root.sh in `HA_CONFIG` context and ends with "privileged user configuration not completed". Fix: `roothas.pl` directly as root — `CLSRSC-327: Successfully configured Oracle Restart for a standalone server`. Docs updated: step 3 stby01 (both methods) now contains `roothas.pl` after `root.sh`. |

**Author:** Claude Sonnet 4.6 | **Date:** 2026-04-28

---

## Session 2026-04-28 (part 4) — DB Software install (prim01/prim02) ✅ DONE

| ID | Status | Files | Description |
|----|--------|-------|------|
| **S28-13** | ✅ DONE | `response_files/db.rsp` | **INS-10105 — invalid RSP for 23.26.1** — `oracle.install.db.runCVUChecks` does not exist in the 23.26.1 schema (old parameter); `oracle.install.db.rootconfig.sudoPassword` — also absent; `oracle.install.db.clusterNodes` → `oracle.install.db.CLUSTER_NODES` (uppercase). Removed the two invalid parameters, renamed clusterNodes. DB software installed on prim01+prim02, root.sh executed on both nodes. |

**Author:** Claude Sonnet 4.6 | **Date:** 2026-04-28

---

## Session 2026-04-28 (part 5) — DBCA DONE + documentation fixes ✅

| ID | Status | Files | Description |
|----|--------|-------|------|
| **S28-14** | ✅ DONE | `response_files/grid_restart.rsp` v3.0 | **Wrong comment** — the previous text claimed that `root.sh` configures OHAS on its own. In reality, for `CRS_SWONLY` `root.sh` performs only basic OS setup; OHAS is configured only by `roothas.pl`. Comment corrected — describes the 2-step procedure: `root.sh` (OS only) → `roothas.pl` (OHAS). |
| **S28-15** | ✅ DONE | `response_files/grid_restart_config.rsp` | **No warning about a non-working approach** — the file documented the `executeConfigTools + HA_CONFIG` method, which fails in 23.26.1, without any warning. Added a `!! DO NOT USE !!` header with the bug history and a pointer to the correct method (`roothas.pl`). |
| **S28-16** | ✅ DONE | `docs/05_Database_Primary.md` | **No DBCA logs in the instruction + no nohup for DBCA** — added a "3. DBCA logs — where to look" section with a table of 3 log locations; Method 1 changed from plain `bash` to `nohup ... &` with `echo PID` and `tail -f`; added a note about the risk of a broken SSH session and the need for `dbca -deleteDatabase` after an interruption. |
| **S28-17** | ✅ DONE | `docs/06_Data_Guard_Standby.md` | **No nohup/screen warning for DGMGRL** — in Method 2 Step 3 added a block-quote with a warning about the risk of a broken SSH session during `CREATE PHYSICAL STANDBY DATABASE` plus an example using `screen`/`tmux`. |
| **S28-18** | ✅ DONE | `docs/06_Data_Guard_Standby.md` (new section 0) | **No DB Software install step on stby01** — Method 1 jumped straight to `create_standby_broker.sh` without a binary install, which is a hard prerequisite (the `dbs/` directory must exist). Added section "0. Prerequisite: install DB software on stby01" with automatic, manual, and verification methods. |
| **S28-19** | ✅ DONE | `response_files/db_stby.rsp` (new file) | **No separate RSP for stby01** — `db.rsp` has `CLUSTER_NODES=prim01,prim02`; using it on the standalone stby01 would cause an attempt to register the home in RAC CRS → error. Created `db_stby.rsp` as a copy of `db.rsp` with empty `CLUSTER_NODES=` and a comment explaining the difference. |
| **S28-20** | ✅ DONE | `docs/05_Database_Primary.md`, `docs/06_Data_Guard_Standby.md` | **Redundant `cp /tmp/... /home/oracle/`** — scripts and RSP files are launched directly from `/tmp/scripts/` and `/tmp/response_files/` (where MobaXterm copies them). Removed all `cp` from both documents. |
| **DBCA** | ✅ DONE | — | Database `PRIM` (CDB + PDB `APPPDB`) created successfully. `LOG_MODE=ARCHIVELOG`, `FLASHBACK_ON=YES`, `FORCE_LOGGING=YES`. |

**Author:** Claude Sonnet 4.6 | **Date:** 2026-04-28

---

## Session 2026-04-28 (part 6) — DB Software install on stby01: OS group fix

| ID | Status | Files | Description |
|----|--------|-------|------|
| **S28-22** | ✅ DONE | `scripts/create_standby_broker.sh`, `scripts/setup_observer.sh` | **mkstore `-p` + `-autoLogin` — both flags invalid in 23.26.1** — `mkstore --help` confirms there is no `-autoLogin` (available: `-createSSO`, `-createLSSO`, `-createALO`). Additionally: `-p PASSWORD` is rejected by mkstore 23.26.1 → `MaxRetryException: Exceeded maximum allowed password retry limit` already on the first invocation (`-create`). The old VMs script (v1.3, line 116) shows the correct method: password through stdin (heredoc). Fix in `create_standby_broker.sh`: all 3 mkstore invocations changed to heredoc + `-autoLogin` → `-createSSO`. Fix in `setup_observer.sh`: `-autoLogin` → `-createSSO` (this script already used heredoc for `-create`/`-createCredential`). |
| **S28-21** | ✅ DONE | `kickstart/ks-stby01.cfg` | **INS-35341 stby01 — oracle is not in the `racdba` group** — `db_stby.rsp` declares `OSRACDBA_GROUP=racdba`, but `ks-stby01.cfg` did not include `groupadd racdba` and did not add oracle/grid to that group. A comparison with `ks-prim01.cfg` (which has `groupadd -g 54330 racdba` + oracle with `racdba,asmadmin,asmoper`) also revealed the absence of `asmadmin,asmoper` for oracle and `racdba` for grid. Fixed 3 lines: added `groupadd -g 54330 racdba`; `useradd grid` → added `racdba`; `useradd oracle` → added `racdba,asmadmin,asmoper` (in line with prim01). Current VM: manual fix `groupadd racdba && usermod -aG racdba,asmadmin,asmoper oracle && usermod -aG racdba grid` as root, new oracle SSH session. |

**Author:** Claude Sonnet 4.6 | **Date:** 2026-04-28

---

## Session 2026-04-28 (part 7) — Data Guard Standby (create_standby_broker.sh) 🔄 IN PROGRESS

| ID | Status | Files | Description |
|----|--------|-------|------|
| **S28-23** | ✅ DONE | `scripts/create_standby_broker.sh` | **Step 0 wallet — mkstore fix works** — After fix FIX-097/S28-22 (heredoc + `-createSSO`), the local DGMGRL wallet creation step passed without errors. |
| **S28-24** | ✅ DONE | `scripts/create_standby_broker.sh` | **`asmcmd pwget` — exit 2 in step 1 (password file)** — `asmcmd pwget --dbuniquename PRIM` run as oracle from the DB home cannot connect to `+ASM1` (it requires the GI environment: grid user + GI_HOME) → exit 2 → `set -euo pipefail` kills the script. Fix: replaced `asmcmd pwget/pwcopy` with `orapwd file=/tmp/pwd/orapwPRIM password="$LAB_PASS" dbuniquename=PRIM force=y`. In the lab SYS=LAB_PASS, so the new file is semantically identical to the one in ASM. |
| **S28-25** | ✅ DONE | `scripts/create_standby_broker.sh` | **`. oraenv` exit 2 — PRIM1 not in /etc/oratab** — `bash -x` revealed: `dbhome PRIM1` returns `/home/oracle` (PRIM1 is missing from /etc/oratab; RAC CRS manages the instances). oraenv was setting `ORACLE_HOME=/home/oracle` and ending with exit 2 → `set -e` killed the script. Fix: removed `. oraenv` from step 1; ORACLE_HOME and PATH are already set correctly from the oracle `.bash_profile`. |
| **S28-26** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **`orapwd dbuniquename` — OPW-00008 exit 255** — the `dbuniquename=PRIM` parameter in `orapwd` works only when the target file is in ASM (not the file system). For `/tmp/pwd/orapwPRIM` (file system) it is disallowed → OPW-00008 → exit 255 → `set -e` killed the script. Fix: removed `dbuniquename=PRIM` from the orapwd call. |
| **S28-27** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **`orapwd` OPW-00029 — password contains "oracle"** — orapwd 23.26.1 rejects passwords containing the word "oracle" (case-insensitive). `Oracle26ai_LAB!` (15 characters) triggers this. Confirmed by test: `orapwd file=/tmp/test.pwd password="$LAB_PASS" force=y` → FAIL. Fix: dropped orapwd; reverted to `asmcmd pwget/pwcopy` with `ORACLE_SID=+ASM1` (connects to the ASM instance directly, without oraenv). |
| **S28-28** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **`asmcmd pwcopy` ORA-27040 Permission denied** — `asmcmd pwcopy` performs the operation through the ASM process (grid user). The `/tmp/pwd` directory created by oracle with default permissions (755, owner: oracle) blocks writes by grid → ORA-19505/ORA-27040. Fix: `chmod 1777 /tmp/pwd` (world-writable + sticky) before pwcopy; removed `chmod 640` (oracle cannot change permissions on a file owned by grid, but reads it through the oinstall group). |
| **S28-29** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **ORA-12514 DGMGRL — SERVICE_NAME without domain** — Oracle 23.26.1 with `db_domain=lab.local` registers services as `PRIM.lab.local` (not `PRIM`). tnsnames with `SERVICE_NAME=PRIM` → the listener does not match → ORA-12514. Known from VMs/FIXES_LOG FIX-040. Fix: `SERVICE_NAME=PRIM.lab.local` and `STBY.lab.local` in tnsnames on prim01/prim02 and stby01; `GLOBAL_DBNAME=STBY.lab.local`/`STBY_DGMGRL.lab.local` in stby01 listener.ora. Also added the STBY credential to the wallet (required for sqlplus /@STBY in step 7). |
| **S28-30** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **ORA-01081 — STBY database already running in step 3** — Oracle Restart on stby01 automatically starts the STBY database after the DB software install. Step 3 invoked `STARTUP NOMOUNT` without first stopping it → ORA-01081. The error did not stop the script (SSH exit code = 0 despite the sqlplus error), so the database stayed in OPEN/MOUNT instead of NOMOUNT → CREATE PHYSICAL STANDBY would have failed. Fix: added `srvctl stop database -db STBY 2>/dev/null || true` and `SHUTDOWN ABORT;` before `STARTUP NOMOUNT`. |
| **S28-31** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **ORA-12514 DGMGRL — SCAN listener does not see PRIM.lab.local** — tnsnames used `HOST=scan-prim.lab.local`. The SCAN listener (192.168.56.31-33) does not have the `PRIM.lab.local` service because `remote_listener` is not set in the PRIM database (PMON registers only with the local listeners). Confirmed: `lsnrctl status` on prim01 shows `PRIM.lab.local` on the local listener; `lsnrctl status LISTENER_SCAN1` → TNS-01101. Fix: changed `ADDRESS` to an `ADDRESS_LIST` containing `prim01.lab.local:1521` and `prim02.lab.local:1521` (node IPs) — the local listeners on both nodes have the service. |
| **S28-32** | ✅ DONE | `scripts/create_standby_broker.sh` | **Preventive FIX-042: reset remote_listener on STBY after CREATE PHYSICAL STANDBY** — after creating the standby, STBY inherits the SPFILE from the RAC primary, which has `remote_listener=scan-prim.lab.local:1521`. stby01 is Single Instance (Oracle Restart) without SCAN — `remote_listener` must be reset, otherwise stby01's PMON tries to register with the SCAN VIP (unreachable from SI). Known from VMs/FIXES_LOG FIX-042. Added step 6b in the script: `ALTER SYSTEM RESET remote_listener` + `SET local_listener` + `ALTER SYSTEM REGISTER`. |
| **S28-33** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **FIX-061 + FIX-075: DGMGRL 26ai — MAINTAINED AS PHYSICAL removed, CREATE PHYSICAL STANDBY does not exist** — `ADD DATABASE STBY ... MAINTAINED AS PHYSICAL` → "Syntax error before MAINTAINED" (removed in 26ai). `CREATE PHYSICAL STANDBY DATABASE` → "Syntax error before PHYSICAL" (never existed as a DGMGRL command). Confirmed via `help create` — only `CREATE CONFIGURATION` and `CREATE FAR_SYNC`. Fix: new 3-phase flow: (1) DGMGRL CREATE CONFIGURATION + ADD DATABASE (without MAINTAINED), (2) RMAN DUPLICATE TARGET DATABASE FOR STANDBY FROM ACTIVE DATABASE with SPFILE SET, (3) DGMGRL EDIT (StaticConnectIdentifier) + ENABLE CONFIGURATION. FIX-043: RMAN auxiliary via SID=STBY + UR=A (NOMOUNT mode, db_domain inactive). |
| **S28-34** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **ORA-00439: feature not enabled: Real Application Clusters** — RMAN DUPLICATE copies the SPFILE from the RAC primary (cluster_database=TRUE). On the SI standby (stby01) Oracle RAC is unavailable → ORA-00439 on STARTUP MOUNT. Known from VMs/FIXES_LOG FIX-041. Fix: added `SET cluster_database = 'FALSE'` to the RMAN DUPLICATE SPFILE clause. Post-RMAN (step 6b): `ALTER SYSTEM SET cluster_database_instances=1` + `instance_number=1 SCOPE=SPFILE` (these cannot be set in RMAN SET — RMAN-06581 in 26ai). |
| **S28-35** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **ORA-16525: broker not yet available** — CREATE CONFIGURATION invoked right after `ALTER SYSTEM SET DG_BROKER_START=TRUE`. The broker starts asynchronously and needs ~20-30 s. Fix: moved CREATE CONFIGURATION + ADD DATABASE from step 6 (before RMAN) to step 6c (after RMAN Duplicate — the broker is certainly up after several minutes of RMAN). Step 6 replaced with `sleep 30`. |
| **S28-36** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **ORA-27106: system pages not available to allocate memory** (additional info: 6508, 2, 3) — RMAN DUPLICATE copies the SPFILE from the RAC primary which has `use_large_pages=ONLY` (set by F-18.B in `dbca_prim.rsp`). stby01 has no HugePages configured (F-18.A applies only to prim01/prim02 via kickstart). After the auxiliary restart with the new SPFILE, Oracle tries to reserve HugePages → none → ORA-27106. Confirmed: `grep HugePages /proc/meminfo` on stby01 → `HugePages_Total: 0`. Fix: added `SET use_large_pages = 'FALSE'` to the RMAN DUPLICATE SPFILE clause. |
| **S28-37** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **ORA-19660 / ORA-19685 / ORA-19845 / ORA-17628 / ORA-01138** during RMAN DUPLICATE — the primary RAC uses OMF (`db_create_file_dest='+DATA'`, `db_recovery_file_dest='+RECO'`). The clone inherits these parameters via the SPFILE. stby01 (XFS, no ASM) → during backupset verify Oracle tries to write to +DATA → "files in backup set could not be verified", "SPFILE could not be verified". Known from VMs/FIXES_LOG **FIX-044**. Fix: added `SET db_create_file_dest='/u02/oradata/STBY'`, `SET db_recovery_file_dest='/u03/fra/STBY'`, `SET db_recovery_file_dest_size='14G'`, `SET control_files='/u02/oradata/STBY/control01.ctl','/u03/fra/STBY/control02.ctl'` to the RMAN SPFILE clause. Additionally: step 3 (NOMOUNT) cleans `/u02/oradata/STBY/*.dbf|*.ctl`, `/u03/fra/STBY/*.ctl`, `onlinelog/*`, `$ORACLE_HOME/dbs/spfileSTBY.ora` from earlier attempts. |
| **S28-38** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **ORA-01138 from TARGET during DBMS_BACKUP_RESTORE.RESTORESETPIECE** (even though both PRIM1+PRIM2 are OPEN — confirmed by `srvctl status database -db PRIM`) — the PRIM tnsnames alias had `LOAD_BALANCE=on`, RMAN target and AUX RPC back hits landed on different RAC nodes in different Active Duplicate steps. DBMS_BACKUP_RESTORE requires a coherent PRIM1+PRIM2 state; cross-instance RPC during RESTORESETPIECE → "database must either be open in this instance or not at all". Pattern analogous to VMs/FIXES_LOG **FIX-085** (LOAD_BALANCE=ON nondeterministic). Fix: changed to `LOAD_BALANCE=off` + `FAILOVER=on` in both tnsnames blocks (on stby01 — step 2; on prim01+prim02 — step 4). All connections now pin to prim01, with fallback to prim02 if prim01 is down. |
| **S28-39** | ⚠️ NOTE | (state issue, not the script) | **PRIM2 stuck in `Mounted (Closed)` after earlier failed RMAN attempts** — `crsctl stat res ora.prim.db -t` showed PRIM1 OPEN but PRIM2 INTERMEDIATE/Mounted, APPPDB OFFLINE on PRIM2. This was the actual root cause of ORA-01138 (although LOAD_BALANCE=off was needed anyway). Fix: `srvctl stop instance -db PRIM -instance PRIM2 -force` + `srvctl start instance -db PRIM -instance PRIM2 -startoption OPEN` + `ALTER PLUGGABLE DATABASE APPPDB OPEN INSTANCES=ALL` + `SAVE STATE INSTANCES=ALL`. Sanity check: always `srvctl status database -db PRIM -verbose` before RMAN — both "Open". |
| **S28-40** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **ORA-16532 in SHOW CONFIGURATION on PRIM after ENABLE CONFIGURATION** — `dg_broker_config_file1/2` by default point to the local `$ORACLE_HOME/dbs/dr1PRIM.dat` and `dr2PRIM.dat`. In RAC, PRIM1 writes the broker config file locally, but PRIM2 sees an empty directory → ORA-16532 "broker configuration does not exist". Fix: in step 5 (before the first `DG_BROKER_START=TRUE`) added `ALTER SYSTEM SET dg_broker_config_file1='+DATA/PRIM/dr1PRIM.dat' SCOPE=BOTH SID='*'` and `dg_broker_config_file2='+RECO/PRIM/dr2PRIM.dat'`. The broker, on startup, will create the files directly in shared ASM storage — both RAC nodes see the same configuration. NOTE: ORA-16573 blocks the change while the broker is enabled → you must REMOVE CONFIGURATION + DG_BROKER_START=FALSE → change → DG_BROKER_START=TRUE → CREATE CONFIGURATION. The script now does this RIGHT AWAY in step 5 — without "dance moves". |
| **S28-41** | ✅ DONE | `scripts/setup_oracle_env.sh` | **ORA-12162 "net service name is incorrectly specified" on `sqlplus / as sysdba` on prim02 and stby01** — `setup_oracle_env.sh` v1.1 set `ORACLE_SID` in `.bash_profile` only for the `grid` user (per node: +ASM1/+ASM2/+ASM), but not for `oracle`. Without ORACLE_SID, sqlplus looks up a network alias from an empty string → ORA-12162. Fix v1.2: setup_profile for `oracle` per host: prim01→PRIM1, prim02→PRIM2, stby01→STBY. Idempotent — re-running the script appends only the missing ORACLE_SID on those nodes. |
| **S28-42** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **ORA-16603 "member is part of another Oracle Data Guard broker configuration" on `ADD DATABASE STBY` from PRIM** — even though PRIM had a fresh configuration (after REMOVE+CREATE), STBY still remembered the old configuration from local files `$ORACLE_HOME/dbs/dr1STBY.dat` and `dr2STBY.dat` (created earlier when `DG_BROKER_START=TRUE` was set in the SPFILE from FIX-S28 SET clause RMAN DUPLICATE). The DMON broker on STBY at startup read the old files → STBY claims "I'm in fsfo_cfg" → ORA-16603 on ADD. Fix: in step 3 (cleanup of previous attempts, FIX-S28-37) added `rm -f $ORACLE_HOME/dbs/dr1STBY.dat $ORACLE_HOME/dbs/dr2STBY.dat` — the files will be created from scratch on the new ENABLE. Manual recovery: `ALTER SYSTEM SET DG_BROKER_START=FALSE` on STBY → `rm dr*STBY.dat` → `ALTER SYSTEM SET DG_BROKER_START=TRUE`. |
| **S28-43** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **Apply Lag growing linearly + ORA-16809/16853/16855/16857 despite a correct DG configuration** — without Standby Redo Logs (SRL) on STBY the broker ships redo only via archived logs (not real-time). In an idle lab a new archived log appears only on a log switch — Apply Lag = (now − last archived log time on PRIM). Plus FSFO requires SRL for ENABLE FAST_START FAILOVER to work. Fix: two new steps added in the script: (1) step 5 (after DG_BROKER_START=TRUE) creates 6 SRL on PRIM in +DATA (3 per thread × 2 RAC threads, idempotent — checks COUNT(*)); (2) new step 6c (between post-RMAN ALTER SYSTEM and DGMGRL CREATE CONFIGURATION) creates 6 SRL on STBY in `/u02/oradata/STBY/onlinelog/srl_t<N>g<N>.log` (XFS, since stby01 has no ASM). DGMGRL ENABLE renumbered to step 6d. After ENABLE: Lag = 0s, Real Time Query = ON, Configuration Status = SUCCESS without warnings. NOTE: ORA-01156 ("recovery in progress") when you try to ADD STANDBY LOGFILE with an active MRP — always APPLY-OFF before adding on a working DG. In the script SRL are created BEFORE ENABLE CONFIGURATION (when MRP is not running). Plus mini-fix S28-41-bis: `cluster_database_instances` on SI throws ORA-02065 → wrapped in `WHENEVER SQLERROR CONTINUE` (instance_number is enough). |
| **S28-44** | ✅ DONE | `scripts/create_standby_broker.sh` | **Idempotency + sanity check for safe re-run of the script**: (a) **0a — Pre-flight PRIM state**: `srvctl status database -db PRIM -verbose` → grep `Instance status: Open`, expects 2 occurrences. If fewer (e.g. PRIM2 in `Mounted (Closed)` after an earlier crash — lesson S28-39) → exit 1 with a recovery instruction. Defensively prevents losing 30 min on RMAN that will return ORA-01138. (b) **0b — Defensive broker reset on PRIM**: `ALTER SYSTEM SET DG_BROKER_START=FALSE SCOPE=BOTH SID='*'` with `WHENEVER SQLERROR CONTINUE` — greenfield no-op, on re-run it stops the broker so step 5 can change `dg_broker_config_file1/2` without ORA-16573 ("attempt to change configuration file for an enabled broker configuration"). Plus: `export ORACLE_SID=PRIM1` moved to the start of the script (before sqlplus in 0b) — greenfield `.bash_profile` has it after S28-41, but defensively we force it for non-interactive runs as well. |
| **S28-45** | ✅ DONE | `docs/07_FSFO_Observers_PL.md`, `scripts/setup_observer.sh` | **3 bugs in docs/07 + setup_observer.sh before launching the Observer stage**: (1) `cp /tmp/response_files/client.rsp /tmp/response_files/client.rsp` (line 16) — same source/target, no-op. Replaced with `ls /tmp/scripts/setup_observer.sh /tmp/response_files/client.rsp` and a comment about the repo→/tmp via MobaXterm convention. (2) `SERVICE_NAME = PRIM_DGMGRL` (without `.lab.local`) in the `PRIM_ADMIN`/`STBY_ADMIN` aliases — Oracle 23.26.1 with `db_domain=lab.local` registers services as `NAME.lab.local` (known FIX-040/S28-29). Without the suffix → `ORA-12514` on `dgmgrl /@PRIM_ADMIN`. Fixed in docs/07 lines 113/118 and setup_observer.sh lines 143/148. (3) Missing `LOAD_BALANCE=off + FAILOVER=on` in the PRIM_ADMIN ADDRESS_LIST — lesson S28-38, deterministic connect to RAC instead of random load-balance. Added to docs/07 and setup_observer.sh. (4) Consolidation: docs/07 line 19 from `bash setup_observer_infra01.sh` to `bash setup_observer.sh` (defaults = master), with a note that `_infra01.sh` remains as a thin wrapper (backward compat). |
| **S28-46** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **STBY not registered in HAS (Oracle Restart) on stby01** — RMAN DUPLICATE does not record the database in HAS. After RMAN: `srvctl status database -db STBY` → `PRCD-1120: resource for database STBY could not be found`, `crsctl stat res -t` on stby01 shows only `ora.evmd` + `ora.ons` (empty minimum HAS, no `ora.stby.db`, `ora.LISTENER` etc.). Consequences: (a) `srvctl modify database -db STBY -startoption 'READ ONLY'` in step 7 throws PRCD-1120 — startoption is not set; (b) after a stby01 reboot the database does NOT come up automatically, you have to manually sqlplus + STARTUP. Fix: new step 7b (between SAVE STATE and srvctl modify) runs `srvctl add database -db STBY -oraclehome ... -spfile $OH/dbs/spfileSTBY.ora -role PHYSICAL_STANDBY -startoption MOUNT -policy AUTOMATIC -domain lab.local`. Idempotent — `\|\| true` on re-run when already registered. After add → srvctl modify works, the database has auto-start MOUNT after reboot, the broker takes over and sets READ ONLY WITH APPLY. |
| **S28-47** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **HAS shows `ora.stby.db OFFLINE OFFLINE` despite a running database** — `srvctl add database` (S28-46) registers metadata only, does not start. The actual database process is running outside HAS (started earlier by RMAN+ALTER DATABASE OPEN). HAS cannot detect the "already running" one — there must be an explicit handoff. Without it, after the first stby01 reboot the database still does not come up (HAS sees the resource OFFLINE and treats it as "stop required"). Fix: new step 7c (after `srvctl modify`) runs the handoff sequence: `EDIT DATABASE 'stby' SET STATE='APPLY-OFF'` (broker stop apply) → `SHUTDOWN IMMEDIATE` (sqlplus on stby01) → `srvctl start database -db STBY` (HAS starts in READ ONLY with the startoption set in S28-46) → sleep 10 → `EDIT DATABASE 'stby' SET STATE='APPLY-ON'`. Final sanity check: `crsctl stat res ora.stby.db -t` must show `ONLINE ONLINE stby01 Open Read Only,STABLE`. |
| **S28-48** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md`, `OPERATIONS.md` | **PRCR-1055 "Cluster membership check failed for node stby01" on `srvctl start database -db STBY`** — CSSD on stby01 was OFFLINE. Diagnosis: `crsctl check css` → `CRS-4530: Communications failure contacting CSS daemon`. Root cause: GI install with `CRS_SWONLY` + `roothas.pl` on stby01 registered the `ora.cssd` resource but does NOT auto-start CSSD after a VM boot. `crsctl stat res -t` showed: `ora.cssd OFFLINE OFFLINE`, `ora.evmd ONLINE`, `ora.ons OFFLINE`, `ora.diskmon OFFLINE`. Manual fix: `crsctl start resource ora.cssd -init` (the `-init` flag is required for low-level cluster resources) → after 15 s CSSD ONLINE → `srvctl start database` worked. Script fix: new step 7a (before `srvctl add database` in S28-46) checks `crsctl status resource ora.cssd -init`; if OFFLINE → `crsctl start resource ora.cssd -init` + sleep 15 + verify. Also added to OPERATIONS.md cold START step 3 (stby01) as CRITICAL before HAS verification. NOTE: as a result, after every stby01 boot CSSD has to be started manually (or add a systemd `oracle-cssd.service` unit that does it automatically — an optional optimisation for the future). |
| **S28-68** | ✅ DONE | `AUTONOMOUS_ACCESS_LOG.md` (Final Validation + Executive Summary) | **Scenario 6 (final validation): all 5 layers PASS** after all tests. OS: 16 PASS 0 FAIL. Cluster: RAC complete (3 SCAN listeners), Oracle Restart complete (cssd + listeners). Broker: SUCCESS, MaxAvailability+SYNC, FSFO Zero Data Loss with 3 observers. TAC: MYAPP_TAC running PRIM1+PRIM2 with LEVEL1 attributes. test_log: 50466 rows total (366 from scen.1, 50000 from scen.4 lag, 100 from scen.3 TAC replay with **0 duplicates**). Step 3 session: 6 scenarios in ~90 min autonomous (vs ~6-8 h manual). 6 actual failovers/switchovers + auto-reinstate flawlessly. All bugs (S28-64..S28-67) fixed along the way, recorded in EXECUTION_LOG_PL + AUTONOMOUS_ACCESS_LOG. Environment 100% operational, 0 data loss. |
| **S28-67** | ✅ DONE | `AUTONOMOUS_ACCESS_LOG.md` (Scenario 4) | **Educational lesson: `FastStartFailoverLagLimit` protects Transport Lag, NOT Apply Lag.** Test: `EDIT DATABASE stby SET STATE=APPLY-OFF` + 50000 INSERT on PRIM → Apply Lag rose to 5 min 10 s, but Transport Lag stayed 0. Broker SUCCESS, `FAILOVER TO stby` worked (did not refuse). Redo arrived at stby (Transport=0), only unapplied — the broker considers the data safe and will finish apply after promote. To really block failover in a test: `EDIT DATABASE PRIM SET STATE=TRANSPORT-OFF` — then redo does not arrive, Transport Lag grows. **In production, the real protection against data loss = Maximum Protection mode** (Maximum Availability is not enough even with LagLimit=0). Bonus: Auto-Reinstate=TRUE worked — after failover, manual `STARTUP MOUNT` on PRIM → broker self-reinstates as standby; `SWITCHOVER TO PRIM` restored RAC=Primary. |
| **S28-66** | ✅ DONE | `AUTONOMOUS_ACCESS_LOG.md` (Scenario 5) | **DGMGRL syntax change in 26ai for MASTEROBSERVER promote.** Attempt `EDIT CONFIGURATION SET MASTEROBSERVER TO obs_dc` (19c syntax) → `Syntax error before or at "MASTEROBSERVER"`. Correct 26ai syntax (from `HELP SET`): `SET MASTEROBSERVER TO <observer-name>;` as a top-level command (not inside EDIT CONFIGURATION). Plus observation: systemd `Restart=on-failure RestartSec=10s` on `dgmgrl-observer-*.service` is **so effective** that it makes it impossible to demonstrate a Master Observer failure via `pkill -9` — the process returns in 10 s, the broker blinks with a ping but does not lose contact long enough to promote. Workaround: `systemctl stop` (requires root@infra01) or an explicit `SET MASTEROBSERVER TO <backup>` (works at any time, proves Multi-Observer redundancy). |
| **S28-65** | ✅ DONE | `docs/09_Test_Scenarios_PL.md` (Scenario 3 validation), `AUTONOMOUS_ACCESS_LOG.md` | **2 service routing bugs after multiple switchovers** revealed during the Scenario 3 (TAC replay) test attempt. (1) **Service `-role PRIMARY` on Oracle Restart (SI) does not auto-stop** after a role swap — when stby (SI) becomes physical standby, `MYAPP_TAC` stays running, JDBC FAILOVER=on connects there and ORA-16000 (RO database). Fix: `srvctl stop service` on stby. (2) **SCAN listener `BLOCKED service`** after a role swap — service registered but BLOCKED, client gets ORA-12514. Fix: `srvctl stop/start service -db PRIM` clean re-registration. **Test result:** 100 INSERT × 1 s, kill -9 SPID on PRIM2 during iter 26 → 3-second gap (replay) → continuation → BATCH COMMITTED 100 rows + 0 duplicates + replayCount=0 visible to the application. Application Continuity Level 1 worked transparently. |
| **S28-64** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md` | **Auto-OPEN APPPDB in READ WRITE after a role swap — `srvctl modify pdb -policy AUTOMATIC -role PRIMARY`.** After a manual switchover (Scenario 1) we discovered that APPPDB stays in READ ONLY on the new primary even though SAVE STATE was run on both DBs. Reason: SAVE STATE preserves the state **per role at a given moment** — when stby was a standby (READ ONLY through Active DG), the state was saved as READ ONLY, and after the swap PRIM as primary also tried to open in READ ONLY → ORA-65054 "CDB read only mode". Classic workarounds: manual CLOSE/OPEN READ WRITE + SAVE STATE after every switchover (clunky), or AFTER STARTUP system trigger. **Modernisation in 26ai:** `srvctl modify pdb` treats the PDB as a CRS resource — with `policy=AUTOMATIC + role=PRIMARY`, CRS opens the PDB in READ WRITE ONLY when the DB is Primary; when the DB is in Standby role, CRS does nothing (Active DG manages READ ONLY). Self-healing: any role change → CRS reacts at the next instance start. One-time configuration per DB (PRIM RAC + STBY Oracle Restart). |
| **S28-63** | ✅ DONE | `docs/08_TAC_and_Tests_PL.md` (Step 5.1) | **`ORA-17868: Unknown host specified.: MYAPP_TAC: Name or service not known`** on `java TestHarness`. The JDBC thin driver does NOT read env vars (including `TNS_ADMIN`) — it reads only JVM system properties. Despite `export TNS_ADMIN=/opt/lab/tns` in `~/.bash_profile`, JDBC was looking up `MYAPP_TAC` as a DNS hostname. Fix: `-Doracle.net.tns_admin=/opt/lab/tns` in the `java` command (before `-cp`). After fix: TestHarness connects via UCP, baseline 30 s test shows SUCCESS on PRIM1+PRIM2 (UCP per-connection load balance). Lesson documented in docs/08 Step 5.1 with a warning. |
| **S28-62** | ✅ DONE | `scripts/setup_cross_site_ons.sh`, `docs/08_TAC_and_Tests_PL.md` (Step 3, 3.a) | **3 gaps in the actual cross-site ONS deploy:** (1) `srvctl: command not found` when run as root — root has no grid env; fix: `su - grid -c "srvctl modify ons ..."`. (2) `ssh oracle@stby01 'sudo bash -s'` assumed NOPASSWD sudo for oracle (none); fix: changed to `ssh root@stby01 'bash -s'` (requires the root SSH key on stby01). (3) `oracle-ons.service` `status=203/EXEC` — onsctl needs LD_LIBRARY_PATH+PATH, not just ORACLE_HOME (Environment= in the unit is not enough); fix: wrapper script `/usr/local/bin/{start,stop}-ons.sh` analogous to S28-54 for the observer. (4) `loglevel` and `useocr` in ons.config are UNKNOWN keys in 26ai (warning `unkown key:` in onsctl ping); fix: removed from ons.config. All 4 fixes in the script and in docs/08 Step 3 + 3.a (with 3.a split into 3.a.1 wrapper + 3.a.2 unit). |
| **S28-61-bis** | ✅ DONE | `docs/08_TAC_and_Tests_PL.md` (Method 1) | **Added a "What the scripts do under the hood" noticebox** in Method 1 — the operator choosing the auto path knows that (a) `setup_tac_services.sh` is idempotent thanks to F-12, post-create verify, auto-registration of stby01; (b) `setup_cross_site_ons.sh` has FIX-040 (-clusterid removed), FIX-082 Gap 2 (ons.config nodes), FIX-083 (systemd persistence) baked in. Goal: transparency for the auto-mode user without requiring them to read the script source. |
| **S28-61** | ✅ DONE | `docs/08_TAC_and_Tests_PL.md` (Step 1, 1.5, 2, 3, 5) | **Audit of docs/08 vs scripts + VMs/FIXES_LOG revealed 5 gaps:** (1) Step 1/1.5 missing a noticebox about `PRCD-1126` idempotency (the F-12 script handles it, manual does not); (2) Step 2 missing a warning about the removed `-clusterid` flag in 26ai (VMs/FIX-040) and a missing firewall port 6200 (VMs/FIX-011); (3) Section 3 missing pre-flight `nc -zv stby01 6200` + `onsctl ping` (VMs/FIX-080 F5/F7); (4) Section 3 missing the rationale for using the `_26ai` SQL variant (`GV$REPLAY_STAT_SUMMARY` removed — VMs/FIX-082 Gap 1); (5) Section 5 missing a CRITICAL warning about `oracle.jdbc.replay.OracleDataSourceImpl` vs `pool.OracleDataSource` (VMs/FIX-084 F1) — without it the client gets ORA-03113 instead of TAC replay. All 5 fixes added as noticeboxes in docs/08. TestHarness.java already has the correct `setConnectionFactoryClassName` + `setValidateConnectionOnBorrow(true)` (confirmed). |
| **S28-60** | ✅ DONE | `docs/07_FSFO_Observers_PL.md` (Section 6.2) | **Section 6.2 (obs_dr on stby01) had only the shortcut "Repeat 6.1 with substitutions"** — inconsistent with the spirit of the manual path (the operator wants to see every step without having to mentally remap). Fix: full steps 6.2.a-e analogous to 6.1 — code blocks copied with `obs_dr`/`stby01` names substituted. docs/07 grows by ~120 lines, but in line with the user's preference for manual readability. |
| **S28-59** | ✅ DONE | `scripts/setup_observer.sh`, `docs/07_FSFO_Observers_PL.md` (Section 6) | **`ADD OBSERVER obs_dc ON HOST 'prim01.lab.local'` throws `Syntax error before or at "OBSERVER"` in 26ai DGMGRL.** The `ADD OBSERVER ... ON HOST '...'` command was 19c-and-earlier syntax — in 26ai the broker auto-registers the observer on `START OBSERVER` (called by the systemd wrapper at `systemctl start`). Master is already Active → the broker assigns the new one as Backup automatically. Fix: removed the dgmgrl `ADD OBSERVER` block from the `else` branch (`OBSERVER_ROLE=backup`), only `systemctl enable --now ${SVC_UNIT}` + `sleep 10` remain. Plus an update of docs/07 section 6 noticebox with the lesson. Related: VMs/FIX-074/075. |
| **S28-58** | ✅ DONE | `docs/07_FSFO_Observers_PL.md` (Method 1 + Section 6) | **docs/07 restructure — backup observers in both methods.** Previous state: Method 1 (auto) described only the Master infra01; Section 6 (backup) was using script invocations anyway — inconsistent with the "step-by-step manual" path. Fix: (1) Method 1 expanded into 4 sub-items: 1.1 master infra01, 1.2 obs_dc prim01, 1.3 obs_dr stby01, 1.4 verification `SHOW OBSERVERS` (3 records); (2) Section 6 rewritten as full manual steps 6.1.a-e (mkdir, tnsnames+sqlnet, wallet 4 cred, wrappers, systemd unit) for `obs_dc` on prim01; 6.2 gives the rule for repeating for `obs_dr` with substitutions; 6.3 verification. The file is now consistent: auto = Method 1 (4 commands total), manual = Method 2 + Section 6 (step-by-step for all 3 observers). |
| **S28-57** | ✅ DONE | `scripts/setup_observer.sh`, `docs/07_FSFO_Observers_PL.md` (Step 4) | **`DGM-16979 Authentication failed` when attaching the observer to PRIM/STBY** despite correct aliases (S28-56). Root cause: SQLNET.WALLET_OVERRIDE=TRUE — Oracle uses wallet credentials instead of asking for a password. The wallet had only PRIM_ADMIN/STBY_ADMIN, `PRIM` and `STBY` were missing. The observer tries `connect /@PRIM` → wallet lookup misses → auth fails. Fix: `ensure_cred PRIM` + `ensure_cred STBY` in the script and docs/07. Symptom visible in the observer log: `[P001] Failed to log into PRIM. Authentication failed.` → broker un-registered the observer → `SHOW CONFIGURATION` showed `ORA-16819: fast-start failover observer not started` even though the systemd unit was active. |
| **S28-56** | ✅ DONE | `scripts/setup_observer.sh`, `docs/07_FSFO_Observers_PL.md` (Step 3) | **Observer started but did not attach to PRIM/STBY** — log `start-observer-obs_ext.sh[..]: ORA-12154: Cannot find alias PRIM in /etc/oracle/tns/obs_ext/tnsnames.ora` + `Failed to attach to PRIM/STBY`. Root cause: the observer's tnsnames had only `PRIM_ADMIN`/`STBY_ADMIN` (port 1522 → LISTENER_DGMGRL → broker control). But after START OBSERVER, the broker returns to the observer the `DGConnectIdentifier` (default = db_unique_name = `PRIM`/`STBY`) and the observer tries `connect /@PRIM`/`/@STBY` over **port 1521** (LISTENER, regular service). No aliases → ORA-12154 → observer "started" but completely dysfunctional (cannot ping the databases, cannot detect failover). Fix: added `PRIM`/`STBY` aliases with PORT=1521 + SERVICE_NAME=PRIM.lab.local / STBY.lab.local — in the script and in docs/07. After fix: observer attach OK, `SHOW OBSERVERS` returns the master record. |
| **S28-55** | ✅ DONE | `scripts/setup_observer.sh`, `docs/07_FSFO_Observers_PL.md` (Step 4) | **Wallet credential idempotency** — `mkstore -createCredential` throws `OracleSecretStoreException: Credential already exists` on script re-run (with `set -euo pipefail` → exit). Fix: `ensure_cred(ALIAS)` helper uses `-listCredential | grep -q ALIAS` as a branch — exists → `-modifyCredential`, missing → `-createCredential`. Plus a simplification: password via `printf '%s\n' | mkstore` instead of nested heredocs (more readable, less escaping). Wallet `-create` protected by `if [ ! -f $WL/cwallet.sso ]`. The same pattern in docs/07 Step 4 non-interactive variant. |
| **S28-54** | ✅ DONE | `scripts/setup_observer.sh`, `docs/07_FSFO_Observers_PL.md` (Step 5) | **`status=203/EXEC` in `dgmgrl-observer-obs_ext.service` after systemctl start** even though `dgmgrl` is executable and Environment ORACLE_HOME is set in the unit. Root cause: `ExecStart=` with embedded single-quotes `'/var/log/.../obs_ext.dat'` inside the double-quoted argument `"START OBSERVER ... FILE IS '...' LOGFILE IS '...'"` — the systemd parser cannot handle this tokenisation (a difference vs `bash`/`/bin/sh`). FIX-074/075 from VMs assumed an older parser version. Fix: wrapper scripts `/usr/local/bin/{start,stop}-observer-${OBSERVER_NAME}.sh` (chmod 755) — bash ENV setup + `exec dgmgrl ...` (quotes handled natively). The systemd unit only uses paths to the wrappers. Test: `systemctl status` → `active (running)`, `dgmgrl /@PRIM_ADMIN "SHOW OBSERVERS"` → 1 record. Plus dgmgrl in ExecStart must have ALL Oracle env vars (ORACLE_HOME, LD_LIBRARY_PATH, TNS_ADMIN, NLS_LANG) — Environment= in the unit is sometimes not enough under sudo/systemd, hence the wrapper exports them explicitly. |
| **S28-53** | ✅ DONE | `scripts/setup_observer.sh`, `docs/07_FSFO_Observers_PL.md` (Step 4) | **`mkstore -create -p <pwd>` in 26ai does NOT work — pwd MUST go through stdin.** First attempt: heredoc `<<'WALLETEOF'` (with apostrophes) blocked `${LAB_PASS}` expansion → `mkstore` got `-p ""` and prompted interactively (`PKI-01002`/`01003`). Second attempt (removing the apostrophes + outer-shell `${LAB_PASS}` expansion on argv) — same thing, because `-p` in 26ai mkstore is IGNORED for `-create`/`-createCredential`. Final fix (VMs/FIX-071 pattern): build a temp script `/tmp/wallet_setup.XXX.sh` (chmod 700, owned by oracle, trap rm -f), pwd goes to mkstore via heredoc `<<MKEOF\n$WP\n$WP\nMKEOF` (2× for `-create`, 1× for `-createCredential`). Plus notes: (a) `cwallet.sso` is AUTO-created by `-create` in 26ai (`-autoLogin`/`-createSSO` optional); (b) docs/07 Step 4 shows TWO variants — interactive (the operator types the password) and non-interactive (stdin heredoc). The pitfall is documented in docs/07 as a noticebox. |
| **S28-52-bis** | ✅ DONE | `kickstart/ks-infra01.cfg`, `docs/02b_OS_Preparation_Manual_PL.md` (section 1.4a + 1.5) | **PRVG-1205 / INS-13014 on infra01 during runInstaller Oracle Client** — infra01 does not have `oracle-database-preinstall-23ai` (it is not a DB host), so kernel sysctl values default to RHEL/OL (file-max=779285, rmem_default=212992, aio-max-nr=65536) instead of Oracle-recommended ones. With `-ignorePrereqFailure` the install completed (dgmgrl OK), but for cleanliness these parameters should be set. Fix: added `/etc/sysctl.d/97-oracle-client.conf` (fs.file-max=6815744, fs.aio-max-nr=1048576, net.core.{r,w}mem_default=262144, rmem_max=4194304, wmem_max=1048576, ip_local_port_range) — in the kickstart ks-infra01.cfg (greenfield) and in docs/02b section 1.4a (manual). Plus fixed Observer paths in both files: `tns/ext` → `tns/obs_ext`, `wallet/observer-ext` → `wallet/obs_ext`, `log/oracle/observer` → `log/oracle/obs_ext` (consistent with OBSERVER_NAME=obs_ext default in setup_observer.sh — finally closed off with S28-51). |
| **S28-52** | ✅ DONE | `response_files/client.rsp` | **INS-10105 cvc-complex-type in `client.rsp` on infra01** — F-23 from FIXES_PLAN_v2_PL added `oracle.install.option=INSTALL_CLIENT` and `oracle.install.client.rootconfig.executeRootScript=false` assuming this was a client analog of the DB schema. Bug: `rspfmt_clientinstall_response_schema_v23.0.0` in 26ai is STRICT and does not have these keys (they exist only in `rspfmt_dbinstall_response_schema_v23.0.0`). Allowed keys (from VMs/FIXES_LOG FIX-070): `oracle.install.responseFileVersion`, `UNIX_GROUP_NAME`, `INVENTORY_LOCATION`, `ORACLE_HOME`, `ORACLE_BASE`, `oracle.install.client.installType`. Plus optional: `SELECTED_LANGUAGES`, `ORACLE_HOSTNAME`, `oracle.install.IsBuiltInAccount`, PROXY_*, `oracle.install.client.oramtsPortNumber`, `customComponents`, `schedulerAgent*`, `drdaas.*`. Fix: rollback to v2.2 — minimal rsp with 5 keys + responseFileVersion. F-23 marked as REVERTED. Lesson: the 23.0.0 client schema ≠ db; every superfluous key = INS-10105. |
| **S28-51** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md`, `docs/07_FSFO_Observers_PL.md` | **MaxAvailability + LogXptMode=SYNC alignment with FastStartFailoverLagLimit=0** — after `ENABLE CONFIGURATION` in the script the broker stayed in `MaxPerformance` (default), but `setup_observer.sh` sets `FastStartFailoverLagLimit=0` (Zero Data Loss). Mismatch: ASYNC apply always has some lag → FSFO blocked on EVERY failure (Scenario 4 from docs/09 would work, but Scenario 2 would fail — failover blocked). The architecture in docs/01 + ARCHITECTURE_DIAGRAMS_PL.md + index_PL.html (`MaxAvailability mode, SYNC redo`) assumes this mode. Fix: new step 6e in the script and in docs/06 (after ENABLE CONFIGURATION): `EDIT DATABASE 'PRIM' SET PROPERTY 'LogXptMode'='SYNC'` + `EDIT DATABASE 'stby' SET PROPERTY 'LogXptMode'='SYNC'` + `EDIT CONFIGURATION SET PROTECTION MODE AS MAXAVAILABILITY`. After fix: Scenarios 1-6 in docs/09 work as described. Plus fixed path inconsistencies in docs/07 (`observer-ext` → `obs_ext` + `/var/log/oracle/observer` → `/var/log/oracle/obs_ext` — consistent with `setup_observer.sh` using `OBSERVER_NAME=obs_ext` as default). |
| **S28-50** | ✅ DONE | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md`, `OPERATIONS.md` | **LISTENER (port 1521) on stby01 as a HAS resource — auto-start after reboot.** After a cold restart `lsnrctl status LISTENER` on stby01 → `TNS-12541: No listener` (the listener did not come up). On PRIM redo cannot ship → `ORA-16778: Redo transport error`. Root cause: the `CRS_SWONLY` install + `roothas.pl` on stby01 does NOT create `ora.LISTENER.lsnr` automatically (unlike prim01/02 where the full GI install with RAC has it natively). The listener was started only manually from DB_HOME (oracle) — lost after a reboot. Fix: step 2 of the script changed — listener.ora for LISTENER (1521) deployed in GRID_HOME on stby01 (grid user, append to existing), `srvctl add listener -listener LISTENER -endpoints 'TCP:1521'` as grid (HAS resource), `srvctl start listener`. tnsnames.ora stays in DB_HOME (oracle, for sqlplus). Recovery pitfall: `srvctl add listener` throws `PRCN-2065: Port 1521 not available` if the manual listener is still running → first `lsnrctl stop` from oracle. After deploy: `crsctl stat res ora.LISTENER.lsnr` → ONLINE ONLINE stby01, broker SUCCESS, RFS receives redo. |
| **S28-49** | ✅ DONE (CRS/HAS-managed) | `scripts/create_standby_broker.sh`, `docs/06_Data_Guard_Standby.md`, `OPERATIONS.md` | **`LISTENER_DGMGRL` (port 1522) — best practice CRS/HAS auto-start.** Fundamental gap: the script did not create this listener at all. Consequences: (a) the broker's StaticConnectIdentifier (PORT=1522) pointed to a non-existent endpoint — at switchover/failover/observers `ORA-12514`/`TNS-12541`; (b) `dgmgrl /@PRIM_ADMIN` from infra01 cannot connect → Multi-Observer impossible. Diagnosis: `lsnrctl status LISTENER_DGMGRL` on 3 nodes → `TNS-01101: Could not find listener name`. **CRS/HAS-managed fix (auto-start after reboot):** (1) Listener.ora in GRID_HOME (grid user) on 3 nodes with the `LISTENER_DGMGRL` definition + `SID_LIST_LISTENER_DGMGRL` mapping `<DB>_DGMGRL.lab.local → SID_NAME` (PRIM1/PRIM2/STBY); (2) `srvctl add listener -listener LISTENER_DGMGRL -endpoints 'TCP:1522'` as grid — cluster-wide in RAC, local in HAS; (3) `srvctl start listener` — auto-restart=always default after `srvctl add`. **Pitfall:** `srvctl add listener -user oracle` throws PRCN-2018 (requires root) — that's why listener.ora is in GRID_HOME and not DB_HOME, the listener is started by the grid user (SID_LIST with `ORACLE_HOME=DB_HOME` works cross-home). The script in step 4a does this for prim01/prim02 (RAC), in 4b for stby01 (HAS), in 4c additionally `crsctl modify resource ora.cssd -attr 'AUTO_START=always' -init` (FIX-S28-48 permanently). Plus a GLOBAL_DBNAME fix in docs/06 (`.lab.local` — S28-29 consistency). After a greenfield re-run everything comes up on its own after a reboot — no manual `lsnrctl start`. |
